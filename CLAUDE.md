# 고리 (aboutmeat.co.kr) — 작업 규칙

축산업 요청·견적 매칭 플랫폼. 빌드 도구 없는 정적 HTML/CSS/JS + Supabase + Vercel.

## 절대 규칙

1. **기존 함수·이벤트 핸들러·Supabase 연동 코드를 지우거나 이름을 바꾸지 않는다.**
   기능을 바꿀 때는 원본을 남기고 바깥에서 감싼다.
   ```js
   var orig = window.gOpenRequest;
   window.gOpenRequest = function(id){ /* 추가 동작 */ return orig.apply(this, arguments); };
   ```
   CDN 이 막히거나 확장이 실패해도 원래 동작으로 돌아갈 수 있어야 한다.

2. **DB 스키마(테이블·컬럼)를 임의로 바꾸지 않는다.** 필요하면 `db/` 에 추가 전용
   SQL 을 새로 쓰고, 왜 필요한지 먼저 설명한다. 기존 값을 바꾸지 말고 클라이언트에서
   매핑한다 (`CATS8[].legacy` 참고).

3. **숫자·거래건수·뉴스·매물을 지어내지 않는다.** 데이터가 없으면 빈 상태를 보여준다.
   확인되지 않은 것을 사실처럼 쓰면 안 된다. (예전에 홈에 "ASF 방역 강화" 같은 가짜
   기사 제목이 있었고 전부 걷어냈다.)

4. **실제로 만들지 않은 기능을 광고하지 않는다.** `site-info.js` 의 `GORI_FEATURES`
   로 끈다.

## 파일을 고치는 방법

| 고칠 것 | 방법 |
|---|---|
| `gori-app.js` | **직접 고치지 말 것.** `src/` 의 조각을 고치고 `node build.js` |
| `index.html` | 고친 뒤 반드시 `cp index.html meat_insight_main.html` (5개 페이지가 이걸 홈으로 링크한다) |
| 사업자 정보·홈 콘텐츠 | `site-info.js` 한 곳 |

`src/` 의 순서는 `build.js` 의 `ORDER` 가 정답이다.
- `07_init.js` 는 **항상 마지막** (IIFE 닫는 괄호가 여기 있다)
- 확장 조각(14~)은 `12_redesign.js` **뒤** — 앞에 두면 리디자인이 덮어쓴다
- 확장 패치는 `07_init.js` 의 `applyExtras()` 에서 **타이머로** 부른다.
  `init()` 안에서 부르면 DOMContentLoaded 가 리디자인(420ms)보다 먼저 오는
  환경에서 감싸는 순서가 뒤집힌다. (실제로 겪은 버그다)
- 홈 히어로·헤더는 `index.html` 마크업이다. **id 와 onclick 을 그대로 두는 한**
  겉모습은 바꿔도 된다 (`hs-input` · `hero-chips` · `gh-stat` · `cat8-grid` ·
  `heroGo` · `heroPick` · `gPickRegion` · `gHeroBell`). 하나라도 이름이 바뀌면
  검색·지역·알림이 조용히 죽는다.
- **`.hdr-actions` 안에는 아무것도 직접 넣지 마라.** `renderHeaderUser()` 가
  로그인 상태가 바뀔 때마다 통째로 다시 쓴다. 헤더에 뭘 붙이려면 `.hdr-right`
  안, `.hdr-actions` **바깥**에 두거나 `44_premium.js` 처럼 그릴 때마다 다시 붙여라.
- `33_layout.js` 는 **`ORDER` 와 `applyExtras()` 둘 다에서 맨 마지막**
  (`07_init` 바로 앞). 그려진 조각을 두 칸으로 담는 일이라, 다른 패치가
  버튼·안내를 다 붙인 뒤여야 그것들까지 같이 담긴다.
- 화면을 옮겨 담는 코드는 **직계 자식만** 보고 판단할 것. `querySelectorAll`
  로 안쪽까지 훑으면 이미 담아 둔 칸(`.lay-side`)을 통째로 집어 날린다.

주의: `renderHeaderUser` · `paintBell` · `renderRequestDetail` · `renderQuotes` ·
`quoteRow` 같은 이름은 IIFE 안의 지역 함수라 **`window.` 에 없다.** 감쌀 때는
지역 바인딩을 직접 재할당해야 한다. `window.X = ...` 로 감싸면 조용히 아무 일도
일어나지 않는다.

## 없는 것을 보여주지 않는다

숫자를 지어내지 않는 것(3번)과 같은 뿌리다. 손님 눈에 **`(미기재)` · `샘플` ·
`TEST` · `미등록` · `지역미정` · `—`** 이 보이면 그 순간 미완성 사이트로 읽힌다.

- 값이 없는 항목은 **자리표시자를 찍지 말고 그 줄을 통째로 뺀다.**
  (푸터 사업자 정보 · 업체 카드 · 지표 줄이 전부 그렇게 되어 있다)
- 무엇이 비었는지는 **콘솔에 남긴다** (`site-info.js`) — 운영자만 본다.
- **띠 하나가 사이트 전체를 죽은 것처럼 보이게 한다.** 예전에는 조회 **하나만**
  실패해도 사이트 전체에 "서버에 연결할 수 없습니다" 가 붙었다. 실시간 띠에
  진짜 요청이 흐르는데 화면 아래에 그 문구가 있었다. 지금은
  `NET_CORE`(`purchase_requests` · `suppliers` · `jobs` · `market_prices`)가
  실패했을 때만 띠를 띄우고, **목록에 진짜 내용이 있으면 아예 안 띄운다**
  (`netHasData`). 알림·후기·채팅 같은 보조 표가 막힌 것은 콘솔에만 남긴다.
  ⚠️ 예시 데이터(`39_demo.js`, id 가 `demo-` 로 시작)는 "내용" 으로 세지
  않는다 — 예시로 채워 놓고 띠를 내리면 장애를 "원래 잘 되고 있다" 로
  보이게 만든다.
- **조회에서 400 이 나면 정렬·조건부터 의심한다.** 오래 전에 만든 표에
  `created_at` 이 없는데 `.order("created_at")` 을 걸면 PostgREST 가 400 을
  준다. 표도 서버도 멀쩡한데 목록이 통째로 비고 "연결할 수 없습니다" 가 뜬다.
  `selectSafe` 는 실패하면 **정렬·조건 없이** 한 번 더 가져오고
  (`SEL_BARE`), `index.html` 의 `loadFromDB` 가 놓친 세 표는
  `27_offline.js` 의 `netRescue()` 가 같은 방식으로 건진다. 순서만 잃고
  목록은 보인다. `insertSafe` 가 예전부터 없는 칸을 빼고 다시 넣던 것과
  같은 생각이다.
- **"서버에 연결할 수 없습니다" 는 진짜 못 붙었을 때만 쓴다.** RLS 가 anon 의
  select 를 막거나 anon 키가 틀리면(42501 · PGRST301 · 401) 서버는 멀쩡한데
  읽을 권한만 없는 것이다. 예전에는 둘을 뭉뚱그려서 (1) 서버가 멀쩡한데
  못 붙었다고 거짓말을 했고 (2) 눌러도 영영 안 되는 "다시 시도" 를 줬다.
  `01_core.js` 의 `isDeniedError()` 로 가려내고, 손님에게는 "지금은 목록을
  불러올 수 없습니다" 까지만, 무엇을 고쳐야 하는지는 `console.warn` 으로
  (`27_offline.js` 의 `netNoteDenied`). `test/fake-sb.js` 의 `errorAll` ·
  `errorTables` 로 두 경우를 따로 흉내 낼 수 있다.
- **운영자에게 할 말을 손님 화면에 찍지 마라.** 문의 화면에 "고객센터 연락처가
  아직 등록되지 않았습니다", 접수 실패 때 "db/phase7_report.sql 실행 필요" 가
  손님에게 보이고 있었다. 손님에게는 "아직 이곳에서 받지 못합니다" 까지만
  말하고, 무엇을 해야 하는지는 `console.warn` 으로 (`32_report.js` 의 `rpFallback`).
- **제목만 남은 빈 상자를 두지 마라.** 커뮤니티 옆칸이 "카테고리" 글자만 달고
  비어 있었다. 채울 것이 없으면 상자째 내리고 본문이 폭을 다 쓴다.
- **"5건" 이라 쓰고 넷만 보여주지 마라.** 세어 본 사람에게는 틀린 숫자다.
  나머지로 가는 길을 붙이거나 숫자를 맞춘다 (`30_suphome.js`).
- 데이터가 없는 구간은 **메인 노출만** 조건부로 끈다. 화면과 라우트는 그대로 둔다
  (`45_main.js` 의 `mnHideEmpty`). 요청 3건 미만 · 업체 0곳이면 홈에서 내린다.
- 예시로 보여주는 카드는 **화면에 "예시" 라고 적는다.** 실제 요청처럼 보이면
  그 자체가 거짓말이다 (`.cse-tag`).

`test/main2026-e2e.js` 가 전부 감시한다.

## 메인은 아홉 구간까지

히어로 · 업종 · 오늘의 축산 정보 · 핵심 서비스 · 이용 방법 · 신뢰 ·
GORI INSIGHT · 브랜드 선언 · 마지막 행동. 이 아홉이 전부다.

구간을 늘리기 전에 **뺄 것부터 찾아라.** 채워서 커 보이는 게 아니라,
필요한 것만 정확히 보여줘서 커 보이는 것이다.

- 같은 이야기를 두 번 하지 마라. 히어로가 "고리가 무엇인지" 를 말하면
  아래에서 큰 브랜드 카피로 한 번 더 말하지 않는다. 브랜드 슬로건은
  히어로에 **하나만** — 실제로 "좋은 고기가 더 좋은 내일을" 과
  "MEAT CONNECTS PEOPLE" 을 같이 뒀다가 뺐다.
- 최종 구조에 없는 구간은 `45_main.js` 의 `MN_OFF` 에 한 줄로 내린다.
  코드와 라우트는 남긴다. 되살릴 때 그 줄만 지우면 된다.
- 데이터로 여는 구간(실시간 요청 · 등록 업체 · INSIGHT)은
  **예시 데이터가 아니라 진짜 데이터**로 판단한다 (`mnHideEmpty` ·
  `ctInfoSection`). 예시로 채운 구간을 메인에 걸면 손님에겐 활발해 보이고
  운영자에겐 실제 유입이 안 보인다.
- 히어로는 기능 대시보드가 아니다. 알림은 헤더, 시·도 필터는 업체 찾기
  화면(`.rgx`)에 있다. **지역 고르기는 제목 문장 안**에 있다 —
  "전국에서 어떤 축산 업체를 찾으세요?" (`.ph-rg`, `#gh-region`).
  히어로 오른쪽 위에 떠 있는 버튼일 때는 카피·검색과 경쟁해서 전체메뉴로
  치웠는데, 문장의 일부가 되면 자리를 안 먹으면서 첫 줄에서 고를 수 있다.
- 페이지가 카드의 연속처럼 보이면 안 된다. 구간마다 표현을 바꾼다 —
  업종=아이콘 줄, 정보=데이터, 서비스=작은 카드, 이용방법=가로 스텝,
  신뢰=아이콘 줄, 브랜드=어두운 전면.

## 확인

```bash
node build.js --check                    # 빌드 최신인지
node --check gori-app.js                 # 문법
cmp index.html meat_insight_main.html    # 미러 일치
node test/run.js                         # 회귀 38종 (playwright 필요)
cd test && node contrast-e2e.js          # 색 대비 전수 (팔레트를 바꾸면 필수)
bash test/rls-test.sh                    # RLS SQL 을 진짜 Postgres 에 실행 (db/ 를 고치면 필수)
```

바꾼 화면은 **데스크톱과 모바일(390px) 둘 다** 실제로 띄워 보고, 가로 스크롤이
생기지 않는지 확인한다.

## 히어로는 밝다 (사진이 생기면 어두워진다)

예전에는 첫 화면이 통째로 검정이었다. 사진 여섯 장(`GORI_HERO`)을 깔 자리인데
비어 있어서 가느다란 선화만 떠 있었고, 손님 눈에는 축산업 사이트가 아니라
**미완성 화면**으로 읽혔다. "없는 것을 보여주지 않는다" 가 히어로에도 걸린다.

- `44_premium.js` 의 `hsHasPhoto()` 가 `GORI_HERO` 에 `img` 가 하나라도
  있는지 본다. 없으면 **밝은 히어로**(흰 바탕 + 질문형 제목), 있으면
  `.has-photo` 를 붙여 **예전의 어두운 사진 히어로**가 그대로 돌아온다.
  두 벌 다 살아 있다 — 한쪽을 죽은 코드로 만들지 마라.
- 어두운 판 전용 규칙은 전부 `.has-photo` 안에 있다 (`.ph-strip` ·
  `.ph-veil` · 흰 글자 · `--brand-dark` · 어두운 칩). 밝은 판을 고칠 때
  `.gh.ph` 를 건드리면 사진 판까지 같이 바뀐다.
- 제목은 **우리 소개가 아니라 손님에게 거는 말**이다. 브랜드 한 줄은
  눈썹글(`.ph-eye`)에만 둔다 — 같은 이야기를 두 번 하지 않는다.
- 히어로 높이를 **키우지 마라.** 예전 `min-height:760px` 탓에 정작 눌러야 할
  업종 칸이 스크롤 뒤로 밀렸다. `test/premium-e2e.js` 의 "업종 칸이 첫
  화면에" 가 반대 방향으로 감시한다 (예전에는 "히어로가 560px 이상" 이었다).

## 요청서는 필수만 먼저 보여준다

"한우" 를 눌러 들어가면 2단계에 한 화면으로 **열다섯 칸**이 쏟아졌습니다.
원육 기준 조건 열 개 + 연락처 다섯. 그런데 정말 필요한 건 **일곱**입니다.
나머지는 안 채워도 등록되는데, 별표 유무 말고는 구분이 없어서 전부 채워야
할 것처럼 보였습니다.

`47_rwease.js` 가 `fieldHtml` 을 감싸 칸마다 `.rwf-req` / `.rwf-opt` 겉옷을
입히고, `gStep2` 뒤에서 선택 칸을 "조건 더 넣기" 안으로 접습니다.

- **지우는 게 아니라 접는 것이다.** 조건을 자세히 쓰고 싶은 사람은 한 번
  눌러 다 펼친다. 접힌 칸의 값도 `readFields` 가 그대로 읽는다.
- 선택 칸은 라벨에 **"선택"** 이라고 적는다 (`.rwf-opt-tag`).
- 새 칸을 `REQ_FORMS` 에 넣을 때 `req` 를 안 주면 자동으로 접히는 쪽이다.
  **필수를 늘리기 전에 정말 없으면 안 되는지 따져라** — 필수가 늘어날수록
  요청이 안 올라온다.
- 히어로 검색어가 칩에 있는 말이면 **칸에 박지 말고 칩을 켠다.**
  "한우" 가 축종 칩에도 있고 "부위 / 품목" 칸에도 박혀서 같은 말이 화면에
  두 번 나왔습니다 (`rwPickChip`). 칩에 없는 말만 예전대로 칸에 들어간다.
- ⚠️ `fieldHtml` 은 IIFE 안의 지역 함수라 `window.` 에 없다. 지역 이름을
  직접 다시 묶어야 한다.

`test/rwease-e2e.js` 가 감시한다.

## 헤더는 1200px 안에 들어간다

`--max:1200px` 는 화면이 아무리 넓어도 그대로다. 그래서 헤더는 **화면 폭이
아니라 1200px** 로 계산해야 한다. 로고 · 메뉴 · 오른쪽 덩이를 합쳐 1152px
(양쪽 24px 여백 제외) 안이어야 한다.

실제로 두 번 넘쳤다. 마지막에는 로그인 상태에서 오른쪽 덩이만 453px 이 되어
**"전체메뉴" 와 "요청 올리기" 가 화면 밖으로 잘려 나갔다.** `document.
scrollWidth` 로는 안 잡혀서 회귀를 통과했다 — 지금은
`test/premium-e2e.js` 가 헤더 안쪽을 직접 잰다 (1440·1280·1200 × 로그인·비로그인).

- 헤더 메뉴는 **넷까지**. 뉴스·커뮤니티는 전체메뉴 · 푸터(모든 화면) ·
  홈 GORI INSIGHT 에 있다.
- 헤더에서 뺀 것은 **갈 길을 남긴다** — 업체 등록·로그아웃은 전체메뉴로
  옮겼다 (`44_premium.js` 의 `hdTrim` · `hdDrawer`). 지우지 않고 숨긴다.
- **알림 종은 하나다.** 로그인하면 `renderHeaderUser()` 가 `.hu-bell` 을
  그리므로, 히어로에서 옮겨 온 `#hdr-tools` 의 종은 내린다.
- `#hdr-tools` 는 `45_main.js` 가 `44_premium.js` **뒤에** 만든다. 그래서
  `mnMoveHeroTools()` 끝에서 `G.hdTrim()` 을 한 번 더 부른다.

## 주소로 들어와도 빈 화면이 아니어야 한다

상세·폼 화면은 `gOpenX()` **안에서** 본문을 그린다. 라우터(`14_router.js`)는
`go(p)` 만 부르므로, 주소창에 `#/chats` 를 치거나 뒤로가기로 돌아오면
**본문이 빈 칸으로 남는다.** 흰 화면 열다섯 개가 그렇게 있었다.

`46_restore.js` 가 `go()` 를 감싸 처리한다.
- 인자 없이 다시 열 수 있는 화면(`chats` `findreq` `prefs` `verify`
  `djnew` `wprof`)은 원래 여는 함수를 다시 부른다.
- id 가 있어야 하는 화면(`sp` `reqd` `quote` `review` `order` `reqedit`
  `chat` `instant` `report`)은 **돌아갈 버튼이 있는 빈 상태**를 그린다.
- 기다리는 260ms 동안은 뼈대(`skelPanel`)를 깐다. 뼈대는 "아직 아무도 안
  그렸다" 는 표시이기도 해서, 시간이 지나도 뼈대 그대로면 그때 빈 상태로 바꾼다.
- 원래 동선을 덮어쓰면 안 된다 — `gOpenRequest()` 는 `go("reqd")` 직후
  `skelPanel(3)` 을 **동기로** 깔기 때문에 안전하다. 새 화면을 만들 때도
  본문을 즉시 한 번 채워라.

**화면을 새로 만들면 `PGS` 에 넣는 것으로 끝이 아니다.** `RT_TITLE` 에 이름을
넣어야 문서 제목과 스크린리더 안내가 "고리" 로만 나오지 않는다 (`sj` `guide`
`report` `contact` `about` 다섯이 그랬다). `test/restore-e2e.js` 가 감시한다.

## 색을 어디에 쓸지

| 토큰 | 쓰는 곳 |
|---|---|
| `--cta` `#B4232C` 딥레드 | **누르는 것 하나**. 헤더 요청 올리기 · 히어로 검색하기 · `.gbtn-p` · 모바일 + 버튼 |
| `--gn` `#C24A0C` 브랜드 | 링크 · 아이콘 · 구간 강조 · hover |
| `--brand-dark` `#F0503F` | 어두운 배경 위 (히어로의 "고리") — `--gn` 은 어두운 데서 3.79:1 이라 모자랍니다 |

빨강을 구간 제목이나 카드 배경까지 넓히지 마라. 늘어나는 순간 이벤트
페이지처럼 보인다. `test/premium-e2e.js` 의 "딥레드는 CTA 에만" 이 감시한다.

### 업종 타일만 예외 — 묶음마다 한 색

`.cs-ic` 열두 칸은 **칸마다가 아니라 묶음마다** 색을 넣는다. 열두 색을 쓰면
앱 서랍처럼 보이고, 한 색으로만 채우면 무엇이 같은 갈래인지 안 보인다.
CATS8 의 대분류를 따라 셋씩 넷이다.

| 묶음 | 칸 | 토큰 | 흰 아이콘 대비 |
|---|---|---|---|
| 원육 | 소고기 · 돼지고기 · 부산물 | `--cs-meat` `#C24A0C` 주황 | 4.91:1 |
| 생산 | 도축장 · 가공업체 · OEM | `--cs-make` `#2F3A44` 차콜 | 11.61:1 |
| 물류·장비 | 물류 · 포장재 · 장비 | `--cs-move` `#1F51AC` 진블루 | 7.42:1 |
| 사업 | HACCP · 인테리어 · 창업·컨설팅 | `--cs-biz` `#1D6B45` 진그린 | 6.48:1 |

- 색값은 `index.html` 의 `--cs-*` 토큰 한 곳에만 둔다. `44_premium.js` 의
  `CS_ITEMS[].g` 는 **어느 묶음인지만** 말한다 (`.cs-g1`~`.cs-g4`).
- **붉은 계열은 넣지 마라.** 딥레드가 CTA 전용이라는 규칙은 여기에도 걸린다.
  회귀는 색상환으로 본다 — 주황 #C24A0C 는 20°, 딥레드 #B4232C 는 356°라
  "빨갛다" 로 뭉뚱그리면 브랜드색까지 걸린다 (실제로 한 번 걸렸다).
- 처음엔 네 색의 밝기를 4.9~6.7:1 로 맞췄는데 **타일이 물러 보였다.** 원육의
  브랜드 주황만 두고 나머지 셋을 한 단계씩 눌렀다. 대비 폭이 벌어져도
  (4.9~11.6:1) 주황이 가장 밝아 원육이 먼저 읽히므로 위계는 유지된다.
  그전에 쓴 `--blue` `#245FCB` 는 대비는 됐지만 채도가 높아 물류 셋만 튀었다.
- 모바일 3열에서는 한 줄이 곧 한 묶음이 된다 — 줄 수를 바꾸면 그 관계가 깨진다.

`test/main2026-e2e.js` 가 네 묶음 · 묶음 안 한 색 · 빨강 없음 · 대비를 본다.

## CSS 를 어디에 쓸지

반대 방향도 있다: `index.html` 에서 `outline:none` 같은 것을 클래스 두 개 이상으로
잡으면 `gori-app.css` 의 `html :focus-visible`(0,1,1)을 이겨서 **초점 테두리가
통째로 사라진다.** 실제로 새 검색창에서 그렇게 지웠다 — `test/a11y-e2e.js` 와
`test/keyboard-e2e.js` 가 잡는다.

`gori-app.css` 는 `index.html` 의 `<style>` **보다 먼저** 실린다. 같은 굵기면
뒤에 오는 `index.html` 이 이기므로, index.html 에 이미 있는 선택자를
`gori-app.css` 에서 다시 잡으면 **조용히 무시된다.** 이길 때는 `html` 을 하나
붙여 굵기를 올린다 (`html .ha-btn{...}`). 실제로 겪은 버그다 — 실시간 띠의
애니메이션을 늦추는 규칙이 통째로 먹히지 않았다.

**비밀번호칸은 반드시 `<form>` 안에 둔다.** 히어로 검색창에 `admin` 이 저절로
박혀 있던 일이 있었다. 이 페이지에는 로그인·회원가입 비밀번호칸이 늘 DOM 에
있는데 **페이지에 `<form>` 이 하나도 없었다.** 폼이 없으면 크롬은 문서 전체를
로그인 폼으로 보고, **화면에 보이는 첫 글자칸**을 아이디 칸으로 골라 저장해 둔
아이디를 넣는다. 그게 히어로 검색창이었다.

`autocomplete="off"` 도, `type="search"` 도 이걸 못 막았다 (둘 다 시도했다).
구조를 고쳐야 한다.

- 로그인 `#l-form` · 회원가입 `#s-form` 으로 감쌌다. `onsubmit` 이 기존
  `doLogin()` · `doSignup()` 을 그대로 부르고 `return false` 로 새로고침을
  막는다. 덤으로 **엔터로도 로그인**이 된다 (전에는 안 됐다).
- 아이디·비번 칸에 `autocomplete="username"` · `"current-password"` ·
  `"new-password"` 를 명시해 크롬이 어디에 넣을지 헷갈리지 않게 한다.
- 검색칸은 `type="search"` + `name` 을 유지하고, 그래도 자동완성이 걸리면
  **그 순간을 잡아 비운다** — `@keyframes gAutofillSeen` 이
  `:-webkit-autofill` 에서만 발화하므로 사람이 친 글자와 헷갈리지 않는다.
- 웹킷이 붙이는 지우기 버튼은 `::-webkit-search-cancel-button{appearance:none}`.

`test/offline-e2e.js` 가 감시한다 — 폼 밖 비밀번호칸이 하나라도 있으면 실패한다.

**사용자가 쓴 글을 화면에 찍을 때는 반드시 `esc()` 를 통과시킨다.**
`gori-app.js` 의 `esc()` 는 IIFE 안에 갇혀 있어 `index.html` 에서 쓸 수 없다 —
그래서 index.html 에 **따로 하나 더** 두었다. 이 파일에서 렌더러를 새로 쓸 때는
그 `esc()` 를 쓸 것. 안 쓰면 업체명에 넣은 `<img onerror=...>` 가 남의
브라우저에서 실행된다. 실제로 16곳이 뚫려 있었다. `test/xss-e2e.js` 가 잡는다.

쓰는 사람 중에 연세 있는 분이 많다. 새로 넣는 글씨는 **12px 미만 금지**,
누르는 것은 **40px 이상**. `test/ease-e2e.js` 가 열 개 화면을 전수로 잡는다.

⚠️ **`<button>` 만 누르는 것이 아니다.** `onclick` 을 단 `li`·`a`·`div` 도
전부 40px 규칙 안이다. 예전에는 회귀가 `button, select, .chip` 만 봐서
**푸터 링크 스물아홉 개가 24px**, 분야 탭이 38px, 시세 안내 링크가 16px 로
남아 있었다. 지금은 `[onclick]`·`a[href]`·`[role=button]` 까지 전부 본다.

`padding` 만으로 높이를 만들지 마라 — "OEM" 처럼 짧은 글자에서 38px 이 된다.
`min-height:40px` + `inline-flex` 로 잡아라.

## 푸터는 모든 화면에 있다

`.footer` 는 `#pg-h` **밖**, 모든 `.pg` 뒤에 있다.

⚠️ `01_core.js` 의 `injectPages()` 와 `32_report.js` 의 `rpInjectPages()` 는
새 화면을 **`.bnav` 바로 앞**에 끼워 넣는다. 푸터도 거기 있으므로, 나중에
만들어지는 화면(요청 상세·견적·당일알바·일감 등록·후기·구직 프로필·신고·문의)이
푸터 **뒤**에 놓이고 그 화면들만 푸터가 맨 위에 찍힌다. `46_restore.js` 의
`rsFooterLast()` 가 푸터를 `.bnav` 바로 앞으로 한 번 옮겨 바로잡는다 —
화면을 새로 주입하는 코드를 더 넣는다면 그 뒤에도 이 함수가 돌아야 한다.
 예전에는 홈 안에 있어서
나머지 서른세 화면에 이용약관·개인정보처리방침·사업자 정보로 가는 길이
아예 없었다 (전자상거래법 제10조 표시 의무는 사이트 전체에 걸린다).
홈의 마지막 두 구간(브랜드 선언·마지막 행동)은 `45_main.js` 가
`#pg-h` 맨 끝에 붙인다 — 푸터를 기준으로 삼지 마라.

## Supabase 라이브러리는 우리 도메인에서 나온다

예전에는 `cdn.jsdelivr.net` 한 줄이었다. 그게 막히거나 느리면 `window.supabase`
가 안 생기고 `sb` 가 null 이 되어 **사이트 전체가** "서버에 연결할 수 없습니다"
가 된다 — 로그인·요청·견적·업체 목록이 한꺼번에 죽는데 원인은 우리 서버도 DB 도
아니다. 그래서 `vendor/supabase-js-2.116.0.min.js` 로 직접 내보낸다.

- **둘 다 "아직 없을 때만" 부른다.** `window.supabase||document.write(...)` 를
  vendor · CDN 두 줄에 각각 건다. 이미 있는 클라이언트를 덮어쓰면 테스트가
  끼워 넣은 가짜가 죽는다 — 실제로 그렇게 깨뜨렸다 (`e2e.js` ·
  `gate-e2e.js` · `admin-find-e2e.js` 세 개가 한꺼번에 실패했다).
- 라이브러리를 쓰는 HTML 은 일곱 개다: `index.html`(+미러) · `admin.html` ·
  `dashboard.html` · `jobs.html` · `meat_insight_apply.html` ·
  `purchase_request.html` · `suppliers.html`. **한 곳만 고치면 안 된다.**
- 올릴 때는 `npm pack @supabase/supabase-js@2` 로 다시 받아 통째로 바꾸고
  파일 이름의 버전도 같이 올린다 (`vendor/README.md`).
- **글꼴은 CDN 을 그대로 둔다.** 못 받아도 글꼴만 대체되지 사이트가 멈추지
  않는다. 다만 `--font` 사슬에 윈도우(맑은 고딕)·맥(애플 SD 산돌) 기본 한글
  글꼴을 넣어 두었다. `gori-app.css` 에 `"Noto Sans KR"` 을 직접 박아 두면
  Pretendard 가 못 떴을 때 버튼·입력칸만 다른 글꼴이 된다 — `var(--font)` 를 써라.

**어디서 막혔는지는 콘솔이 말해 준다** (`27_offline.js` 의 `netDiagnose`) —
라이브러리가 안 뜬 것인지, 클라이언트를 못 만든 것인지, 서버가 응답이 없는
것인지 세 가지로 나눠 `console.warn` 한다. 예전에는 전부 같은 문구였다.

`test/offline-e2e.js` 가 감시한다.

## 운영자가 해야 할 설정

`README.md` 의 "남은 것 · 켜야 할 것" 표를 볼 것. 특히 **RLS**(`db/phase4_admin.sql`
5번 블록)는 켜지 않으면 anon 키로 데이터를 지울 수 있다.
