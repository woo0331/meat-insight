# ABOUTMEAT (aboutmeat.co.kr) — 작업 규칙

소·돼지 **부산물 전문 온라인몰**. 빌드 도구 없는 정적 HTML/CSS/JS + Vercel.
`index.html` 을 그냥 열면 바로 돕니다. 설치할 것도, 빌드할 것도 없습니다.

> **2026-09-22 — 전면 리뉴얼.** 예전에 이 주소에는 축산업 요청·견적 매칭
> 플랫폼 "고리" 가 있었습니다. 서비스를 끝내고 파일도 전부 지웠습니다
> (192개). 되살릴 일이 있으면 `git log -- src/` 로 찾을 수 있고,
> 그 시절 작업 규칙은 `git show 2954760:CLAUDE.md` 에 있습니다.

## 구조

```
index.html              SPA 껍데기 (스크립트 순서가 곧 의존 순서)
build-pages.js          주소마다 HTML 파일을 만듭니다 (검색엔진용)
c/ p/ enc/ products/ …  build-pages.js 가 만든 것 — 손으로 고치지 마세요
css/tokens.css          색·글씨·간격·모서리·비율 — 날값은 여기 한 곳에만
css/app.css             공통 (헤더·히어로·카드·버튼·푸터·모바일 네비)
css/pages.css           화면별 (목록·상세·도감·장바구니·폼)
js/data/korean.js       조사(은/는·이/가·을/를) 고르기
js/data/site.js         사업자 정보 (WOW_BIZ)
js/data/categories.js   소·돼지 전체 분류 · GNB · 퀵 카테고리
js/data/products.js     상품
js/data/filters.js      필터·정렬
js/data/encyclopedia.js 부산물 도감
js/data/legal-terms.js  이용약관 본문
js/data/legal-privacy.js 개인정보처리방침 본문
js/components/          base · chrome · cards · diagram
js/pages/               home · list · detail · misc · legal
js/app.js               라우터 · 장바구니 · 찜
img/                    사진 48장
check.js                전수 점검
admin.html              상품·사업자 정보 편집기 (js/admin.js · js/admin-ui.js)
404.html · robots.txt · sitemap.xml · manifest.json · og.jpg · sw.js
```

`vercel.json` 에는 보안 헤더와 **rewrite** 가 있습니다. 새로 넣은 상품처럼
아직 HTML 파일이 없는 주소를 `index.html` 로 보내 줍니다.

## 절대 규칙

1. **숫자·후기·재고를 지어내지 않는다.** 없으면 "아직 등록된 리뷰가
   없습니다" 라고 씁니다. `0` 도 숫자입니다 — 모르는 것을 `0` 으로 적지
   마세요.
2. **값이 없는 항목은 자리표시자를 찍지 말고 줄째 뺀다.** 푸터 사업자
   정보(`js/data/site.js` 의 `WOW_BIZ`)가 그렇게 되어 있습니다. 손님 눈에
   `(미기재)` · `undefined` 가 보이면 그 순간 미완성 사이트로 읽힙니다.
3. **운영자에게 할 말을 손님 화면에 찍지 않는다.** 손님에게는 "지금은
   이곳에서 받지 못합니다" 까지만, 무엇을 설정해야 하는지는
   `console.warn` 으로 (`doLogin` · `checkout` · `submitQuote` 가 그렇게
   되어 있습니다).
4. **사용자가 쓴 글은 반드시 `esc()` 를 통과시킨다** (`js/components/base.js`).
   안 쓰면 상품명에 넣은 `<img onerror=...>` 가 남의 브라우저에서 돕니다.
5. **데이터를 UI 에 하드코딩하지 않는다.** 상품·분류·필터·도감은 전부
   `js/data/` 에 있습니다. 화면 코드에 "곱창" 같은 말을 직접 적지 마세요.

## 색을 어디에 쓸지

| 토큰 | 쓰는 곳 |
|---|---|
| `--green` `#123D32` 딥그린 | 브랜드 전면. 헤더 로고 · 버튼 · 어두운 배너 · 푸터 |
| `--ivory` `#F6F4EE` | 바탕. 흰색 구간과 번갈아 칠합니다 |
| `--red` `#C85B4B` | **가격 · 특가 · NEW 배지에만** |

⚠️ **빨강을 구간 제목이나 카드 배경까지 넓히지 마세요.** 늘어나는 순간
"전통시장 정육점" 으로 읽히고, 그건 이 사이트가 피해야 할 방향입니다.
아이보리 위의 가격 글자는 `--red` 가 아니라 `--red-ink`(#A8402F)를 씁니다 —
본문 대비를 맞추기 위해서입니다.

## 글씨와 누르는 것

쓰는 사람 중에 정육점·국밥집 사장님이 많습니다. kg 단가를 눈으로 읽고
손가락으로 누릅니다.

- **12px 미만 금지.** 제일 작은 단계가 `--t-cap` 12.5px 입니다.
- **누르는 것은 40px 이상.** `<button>` 만이 아니라 `onclick` 을 단
  `a`·`li`·`div` 도 전부 해당합니다.
- ⚠️ **`padding` 만으로 높이를 만들지 마세요.** "천엽" 처럼 짧은 이름에서
  다시 낮아집니다. `min-height:40px` + `flex` 로 잡으세요.
  (상품명 `.pc-nm` 이 실제로 24px 이었습니다)
- ⚠️ **이름표를 SVG `<text>` 로 넣지 마세요.** 그림이 줄면 글자도 같이
  줄어 모바일에서 11.5px 이 됐습니다. HTML 로 빼세요
  (`js/components/diagram.js` 의 `.pd-cap`).
- ⚠️ **문장 한가운데 링크를 박지 마세요.** 높이가 16px 이라 누르기
  어렵습니다. 문단 끝에 버튼으로 내놓으세요.

## 모르는 것을 "없다" 고 적지 마세요

절대 규칙 1번의 다른 얼굴입니다.

- 마이페이지가 **로그인도 안 한 손님에게 "주문 내역이 없습니다"** 라고
  단정하고 있었습니다. 우리는 그 사람이 누구인지 모르므로 주문이 있는지
  없는지도 모릅니다. 지금은 `isLoggedIn()` 을 보고 "로그인이 필요합니다"
  로 바꿉니다. **찜한 상품만 예외** — 로그인과 무관하게 이 브라우저에
  저장되므로 실제로 알 수 있습니다.
- ⚠️ `isLoggedIn()` 을 인증 없이 true 로 만들지 마세요. "로그인한 척"
  하면 마이페이지가 남의 주문을 보여주는 것처럼 읽힙니다.

## 눌러도 아무것도 없는 칸을 두지 마세요

- 소 부산물 화면에서 축종 다섯 중 넷, 상품상태 둘 중 하나, 손질상태
  여섯 중 다섯이 **전부 0건**이었습니다. 눌렀더니 빈 목록이 나오는 칸은
  손님에게 고장으로 읽힙니다. 분류 칩(`CatChips`)에는 이미 이 규칙이
  있었는데 필터만 빠져 있었습니다.
- 도감 상세도 같습니다. 부위 48개 중 상품이 있는 것은 16개뿐이라,
  없을 때는 **없다고 말하고 갈 곳을 줍니다** (분류 보기 · 대량견적 ·
  전화). ⚠️ 이때 **분류에 상품이 있는지 먼저 보고** 없으면 축종 전체로
  보냅니다 — `/c/beef/bone` 은 상품이 0건이라 누르면 빈 목록입니다.
- 지금은 `fltCount()` 로 **개수를 같이 적고, 0 이면 못 누르게** 합니다.
  묶음 안이 전부 0 이면 묶음째 내립니다 — 제목만 남은 빈 상자를 두지
  않습니다.
- ⚠️ 개수는 **같은 묶음 안의 다른 선택을 빼고** 셉니다. 한 묶음 안은
  OR 로 걸리므로, "한우" 를 고른 상태에서 "육우" 가 0 으로 보이면
  안 됩니다.

## 가입에는 동의가 필요합니다

이용약관은 약관규제법상 명시·설명 의무가, 개인정보 수집·이용은
개인정보보호법 제22조상 동의 의무가 있습니다.

- **필수와 선택을 반드시 나눠 받습니다.** 광고 수신까지 묶어 필수로
  받으면 그 동의는 무효입니다. 라벨에 "(필수)" · "(선택)" 을 글자로
  적으세요 — 별표만으로는 손님이 구분하지 못합니다.
- 무엇에 동의하는지 읽을 수 있어야 하므로 **약관·방침으로 가는 길**을
  같이 둡니다 (`.ag-v`).
- `doSignup()` 에 인증을 붙일 때 **동의 확인 코드를 지우지 마세요.**
- ⚠️ **방침이 폼과 어긋나면 안 됩니다.** "사업자등록증 사본을 수집한다"
  고 적어 두었는데 폼에는 그 칸이 없었습니다. 수집 항목을 바꾸면
  `legal-privacy.js` 제2조를 같이 고치세요.

## 부산물 장사의 사정

- **품절이 잦습니다.** 당일 수급이라 아침마다 열댓 개가 바뀝니다.
  `soldOut` 을 켜면 사진이 흐려지고 배지가 붙고 **담기가 사라집니다.**
  숨기지는 않습니다 — 없어진 줄 알고 딴 데 가는 것보다 있다는 걸 알고
  다음에 오는 편이 낫습니다. 관리자 **목록에서 바로** 켜고 끕니다
  (상품마다 편집 화면에 들어갔다 나오면 아침에 못 씁니다).
  ⚠️ 담기는 길이 여러 군데(카드·상세·바로구매)라 `addCart()` 에서도
  한 번 더 막습니다. 한 곳만 막으면 샙니다.
- **"몇 시까지 주문하면 언제 받는지" 가 구매를 정합니다.** 곱창집
  사장님은 내일 장사에 쓸 것을 오늘 삽니다. `shipCutoff` · `shipNote` ·
  `shipExclude` 를 채우면 상품 상세와 장바구니에 나옵니다. 안 채우면
  **아예 안 나옵니다** — 마감 시간을 모르면서 "내일 도착" 이라고 쓸 수는
  없습니다.
- **전화로 삽니다.** `phone` 을 채우면 상품 상세·장바구니·품절 안내에
  전화 버튼이 붙습니다. 번호가 없으면 버튼이 안 나옵니다 — 눌렀는데
  안 걸리는 것보다 없는 게 낫습니다.
- ⚠️ **배송비를 화면에 박지 마세요.** 장바구니에 `100000` · `3000` 이
  박혀 있었습니다. 관리자에서 기준을 바꿔도 장바구니만 옛날 숫자를
  말합니다. `WOW_BIZ.shipFee` · `shipFreeOver` 에서 읽습니다.

## "곱창을(를)" 이라고 쓰지 마세요

조사를 괄호로 때운 자리가 **카테고리 17개의 검색용 설명**에 그대로
들어가 있었습니다. 화면에서만 어색한 게 아니라 **구글 검색 결과 줄에
그대로 나갑니다** — 그 한 줄이 자동 생성 티를 냅니다.

- `josa(낱말, "은는")` 를 씁니다 (`js/data/korean.js`). 받침을 보고
  고릅니다. `koWith("사골","은는")` → `"사골은"`.
- ⚠️ **이 파일은 `js/data/` 에 있어야 합니다.** `build-pages.js` 는
  `js/data/*.js` 와 `app.js` 의 `routeInfo()` 만 읽습니다.
  `components/` 에 두면 화면은 멀쩡한데 **검색용 메타에만** 괄호가
  남습니다. 실제로 그렇게 됐던 자리입니다.
- ⚠️ **"와/과" 만 순서가 거꾸로입니다.** 은/는·이/가·을/를 은
  받침 있는 쪽이 먼저인데 와/과 는 아닙니다. `KO_PAIR` 가 `"와과"` ·
  `"과와"` 둘 다 같은 답을 내도록 받아 둡니다.
- `check.js` 가 `은(는)` · `을(를)` 꼴을 개발자 말로 잡습니다.

## 도감 사진이 없으면 그림을 넣습니다

부위 48개 중 사진이 있는 것은 19개뿐입니다. 나머지를 어떻게 할지가
문제였습니다.

- ⚠️ **없는 파일명을 적어 넣지 마세요.** `imgTag` 가 404 를 한 번 내고
  카드가 빈 상자가 됩니다.
- ⚠️ **비슷하게 생겼다고 남의 부위 사진을 돌려 쓰지 마세요.** 곱창
  사진이 붙은 사골은 거짓말이고, 손님은 그 사진으로 살지 말지를
  정합니다. 절대 규칙 1번과 같은 이야기입니다.
- 지금은 `img` 를 **아예 빼면** `PartThumb()` 이 부위 위치 그림을
  대신 넣습니다. 상세 화면에서는 사진 칸을 **줄째 뺍니다**
  (`.enc-d-solo`).
- ⚠️ **돼지에 양·벌집양·천엽은 없습니다.** 위가 하나뿐입니다. 소의 네
  개 위를 그대로 옮겨 적은 "돼지 천엽" 한 줄이 들어가 있었습니다.

## 지운 주소의 파일이 남습니다

"돼지 천엽" 을 지웠더니 `sitemap.xml` 에서는 빠졌는데
`enc/pork/cheonyeop/index.html` 은 **그대로 남았습니다.** 주소가 살아
있으니 예전에 색인된 손님과 크롤러는 계속 그 화면을 봅니다.

`build-pages.js` 가 이제 만들고 난 뒤 **이번에 안 만든 `index.html` 을
치웁니다.** 훑는 곳은 만든 주소의 맨 윗 칸(`c` · `p` · `enc` ·
`products` …)뿐입니다 — `css` · `js` · `img` 는 건드리지 않습니다.

## 검색은 이름만 뒤지면 안 됩니다

손님은 "소곱창" · "탕거리" · "국밥" · "위장류" 처럼 **우리가 붙인 분류
이름**으로도 찾습니다. 실제로 "곱창" 을 넣으면 한 건만 나왔습니다 —
대창·막창은 같은 위·장류인데 이름에 "곱창" 이 없어서 빠졌습니다.

- 찾는 곳: 상품명 · 원산지 · 용도 · 축종 · 보관 · 손질 상태 ·
  축종 구분 · 분류명 · 세부 품목명 · 도감 설명
- **띄어쓰기와 가운뎃점·괄호를 지우고** 봅니다. 분류가 "위·장류" ·
  "간 (소간)" 이라 그냥은 "위장류" · "소간" 이 안 잡힙니다.
- ⚠️ **한 글자 검색을 넓게 보면 안 됩니다.** "소" 가 도감 설명의
  "고**소**함" 에 걸려 돼지 상품까지 나왔습니다. 한 글자일 때는
  이름·분류처럼 짧고 정확한 것만 봅니다 (`hayCore` / `hayFull`).
- **가까운 것부터 내놓습니다** (`wowScore`). "곱창" 에 한우 소곱창이
  먼저여야지, 추천요리에 "곱창전골" 이 적힌 양이 먼저 나오면 안 됩니다.

## 필터가 화면보다 길면 안 됩니다

칸 27개를 다 펼쳐 두니 필터가 **995px** 이 됐습니다. 붙여 둔(sticky)
보람도 없고, 목록보다 길어서 아래에 빈 땅이 141px 생겼습니다.

- **묶음째 접습니다.** 앞의 둘과 **고른 것이 있는 묶음**은 펼쳐 둡니다 —
  내가 무엇을 걸어 뒀는지가 접혀 있으면 안 됩니다. 접힌 묶음에는 고른
  개수를 배지로 적습니다. (547px 로 줄었고 빈 땅이 없어졌습니다)
- 0건인 칸은 "지금 없는 조건 N개" 안으로 한 번 더 접습니다.

## 실제로 겪은 버그 — 다시 만들지 마세요

- **사진 url 을 CSS 변수로 넘기지 마세요.** 변수 안의 `url()` 은 HTML 이
  아니라 **그 CSS 파일 기준**으로 풀립니다. `img/hero.jpg` 가
  `css/img/hero.jpg` 가 되어 히어로가 통째로 검게 비었습니다.
  `.hero-ph` 처럼 실제 요소에 인라인 `background-image` 로 주세요.
- **`.w` 에서 `width:100%` 를 빼지 마세요.** 그리드 칸 안에서
  `margin:0 auto` 가 stretch 를 꺼버립니다. 히어로 글자가 1280px → 441px
  로 쪼그라들어 화면 한가운데로 몰렸습니다.
- **그리드 칸에는 `min-width:0`.** 기본값이 `auto` 라, 안에 가로로 긴
  것(스크롤 되는 분류 줄)이 있으면 칸이 화면보다 넓어져 페이지 전체에
  가로 스크롤이 생깁니다. 390px 도감에서 옆칸이 400px 이 됐습니다.
- **어두운 배경 위에서 `.btn-g`(딥그린)는 사라집니다.** 히어로 왼쪽 판이
  `--green` 이라 "소 부산물 보기" 버튼이 배경에 묻혔습니다.
  `.hero .btn-g` 가 한 칸 밝은 초록 + 옅은 테두리로 띄웁니다.
- **`SIL` 은 `<path>` 요소가 아니라 좌표(`d`) 문자열입니다.** 그냥 넣으면
  글자로 들어가 해부도가 통째로 안 나옵니다.
- **이미지 이름이 데이터와 한 글자라도 다르면 카드가 빈 상자가 됩니다.**
  `quick-beef` 와 `q-beef.jpg` 가 어긋나 퀵 카테고리 여덟 장이 전부
  비었습니다. 컷아웃(`q-*` · `e-*`)은 PNG, 사진은 JPG 이고
  `imgExt()` 가 앞글자로 확장자를 고릅니다.

## 사진

`img/` 48장은 **시안 이미지에서 잘라낸 임시 사진**입니다. 시안 원본이
1312px 이라 화소가 그만큼밖에 없어서, 큰 배너는 `background-size:auto 100%`
로 확대율을 낮춰 씁니다. 실제 촬영본이 생기면 **같은 파일명으로** 바꿔
넣으면 코드는 안 고쳐도 됩니다.

⚠️ **글자가 박힌 이미지를 쓰지 마세요.** 시안의 한글은 AI 가 만든 것이라
글자가 틀려 있습니다. 그래서 글자가 걸린 영역은 잘라내지 않았고, 화면의
모든 글은 HTML 로 다시 썼습니다.

## 주소는 진짜 경로입니다 (해시가 아닙니다)

예전에는 `#/c/beef` 였습니다. 화면은 잘 돌았지만 **검색엔진에 안
잡혔습니다** — 해시 뒤는 서버로 전송되지 않아서, 크롤러 눈에는 상품이
몇 개든 주소가 `/` 하나뿐입니다.

지금은 `/c/beef` 이고, **주소마다 실제 HTML 파일**이 있습니다.

```
node build-pages.js      # 페이지 103개 + sitemap.xml 을 만듭니다
                         # (없어진 주소의 파일도 같이 치웁니다)
```

- 상품을 바꾸면 다시 돌리세요. 안 돌려도 화면은 정상입니다
  (`vercel.json` 의 rewrite 가 `index.html` 로 보냅니다) — 다만 그
  상품만 검색용 제목·설명이 기본값이 됩니다.
- 자산 경로는 반드시 절대 경로(`/js/…` · `/img/…`)여야 합니다.
  상대 경로면 `/c/beef/gut` 에서 `/c/beef/gut/js/…` 를 찾아 스크립트가
  통째로 404 가 됩니다. 해시 주소일 때는 경로가 늘 `/` 라 안
  드러났습니다 — 실제로 이걸로 화면이 안 떴습니다.
- 링크는 `href="/c/beef"` 로 씁니다. click 가로채기가 새로고침 없이
  넘깁니다. 새 탭·다운로드·외부 링크·`/admin`·`/api` 는 안 건드립니다.

## 화면을 새로 만들 때

`js/app.js` 한 곳에서 다 합니다.
1. `META` 에 제목과 **설명**을 넣습니다. 설명을 비우면 구글이 본문에서
   아무 문장이나 가져다 씁니다.
2. `routeInfo()` 에 어떤 화면인지 추가합니다.
3. `render()` 의 switch 에 그리는 함수를 답니다.
4. `build-pages.js` 의 `allRoutes()` 에 주소를 넣어야 HTML 파일이
   만들어집니다. 안 넣으면 열리기는 하지만 검색에 안 잡힙니다.

사람마다 내용이 다른 화면(장바구니·로그인·마이페이지)과 검색 결과는
`NOINDEX` 에 넣으세요. 검색 결과 주소는 무한히 생깁니다.

## 구조화 데이터

상품 상세에 `Product` 스키마를 넣습니다 (`ldFor()`). 구글 쇼핑 검색에
가격과 재고가 같이 나옵니다.

- 화면에 없는 값을 적지 마세요. 후기가 0이면 `aggregateRating` 을 넣지
  않습니다 — 없는 별점을 검색 결과에 띄우는 셈입니다.
- 품절은 `OutOfStock` 으로 나갑니다. `InStock` 으로 두면 클릭해 들어온
  손님이 품절을 봅니다.

## 확인

```bash
node check.js        # 21개 화면 × 1440·1024·390px 전수 (playwright 필요)
```

보는 것: JS 에러 · 못 불러온 파일 · 가로 스크롤 · **12px 미만 글씨** ·
**40px 미만 누름** · 낱말 가운데 잘림 · 손님 화면에 남은 개발자 말 ·
링크 100개가 실제로 열리는지.

바꾼 화면은 **데스크톱과 모바일(390px) 둘 다** 실제로 띄워 보세요.

## 가리키는 곳이 실제로 있는가

화면에 안 보이는 거짓말도 있습니다. 파일을 지우거나 이름을 바꾸면
`sitemap.xml` · `robots.txt` · `manifest.json` 을 같이 보세요.
`robots.txt` 로 막아 둔 주소를 `sitemap.xml` 에 올리면 서로 어긋납니다.

`sitemap.xml` 은 `build-pages.js` 가 만듭니다 — **손으로 고치지
마세요.** 지금 99개 주소가 올라가 있고 전부 실제 HTML 파일이 있습니다.
사람마다 내용이 다른 화면(`NOINDEX`)은 빠집니다.

⚠️ **아이콘·미리보기 사진도 가리키는 곳입니다.**
- `og:image` 는 **주소 전체**(`https://…/og.jpg`)여야 합니다. `/og.jpg`
  로 두면 카카오톡·페이스북 미리보기에 사진이 안 뜹니다 — 크롤러는
  우리 도메인을 모르는 채로 그 값을 읽습니다.
- `rel="icon"` 을 안 적으면 브라우저가 `/favicon.ico` 를 찾습니다.
  파일이 없으면 **모든 화면에서 404 가 한 번씩** 납니다.
- `manifest.json` 의 `shortcuts` 에 **해시 주소가 남아 있었습니다**
  (`/#/c/beef`). 홈 화면에 추가한 손님이 눌렀을 때 메인으로 갑니다.
  주소 규칙을 바꾸면 `manifest.json` 도 같이 보세요.

⚠️ **`sw.js` 를 지우지 마세요.** 예전 사이트의 서비스 워커가 손님
브라우저에 설치돼 있습니다. 파일이 404 가 되면 브라우저는 "갱신 실패" 로
보고 **옛 워커를 그대로 계속 씁니다.** 지금 `sw.js` 는 캐시를 비우고
스스로 등록을 해제하는 일만 합니다. 충분히 시간이 지난 뒤에 지우세요.

## 약관과 개인정보처리방침

본문은 `js/data/legal-terms.js` · `js/data/legal-privacy.js` 에 있습니다.
화면(`js/pages/legal.js`)은 그리기만 하니 문구는 데이터만 고치세요.

- **신원 정보를 본문에 박지 마세요.** 상호·대표·사업자등록번호는 문서 맨
  끝 표 한 곳에서만 `WOW_BIZ` 를 읽어 보여 줍니다. 본문은 "회사" 로만
  씁니다 — 그래야 `WOW_BIZ` 하나만 채우면 푸터·약관·방침이 같이 따라옵니다.
- **빈 값은 "(미기재)" 를 찍지 않고 그 줄을 뺍니다.** 하나도 없으면
  "서비스 준비가 끝나는 대로 게시합니다" 한 줄로 바꿉니다. 무엇이
  비었는지는 `console.warn` 으로 — 운영자만 봅니다.
- ⚠️ **결제대행·배송·문자발송 업체를 붙일 때마다** `legal-privacy.js` 의
  `trustees` 에 한 줄씩 추가하세요. 위탁 현황을 안 적으면 개인정보보호법
  제26조 위반입니다. 국외 서버를 쓰면 네 번째 칸에 국가를 적습니다.
- ⚠️ **제15조(청약철회 제한)를 함부로 넓히지 마세요.** 신선식품이라
  제한이 인정되지만, 인정 범위는 업종·사안마다 다릅니다. 지금은
  "개봉해 냉장이 해제된 경우 · 요청에 따라 추가 가공한 경우 · 보관
  부주의로 신선도가 떨어진 경우" 셋으로 좁혀 두었고, **하자·오배송은
  그대로 철회 가능**하다고 제5항에 따로 적었습니다.
- **이 두 문서는 제가 쓴 초안입니다.** 판매를 시작하기 전에 변호사나
  한국소비자원 표준약관과 대조해 확인하세요.

## 관리자 (admin.html)

서버가 없으므로 **DB 를 고치는 것이 아니라 `js/data/products.js` 를
만들어 주는 도구**입니다. 내보낸 파일로 덮어쓰고 올려야 반영됩니다.

- ⚠️ **"여기서 고쳐도 사이트는 안 바뀐다" 는 안내를 지우지 마세요.**
  그걸 모르면 상품을 잔뜩 넣어 놓고 사이트는 그대로인 일이 생깁니다.
  내보내지 않은 변경이 있으면 띠가 뜨고 창을 닫을 때 붙잡습니다.
- **검사가 이 도구의 값어치입니다.** id 중복 · 없는 분류 · 틀린 사진
  이름을 막습니다 — 전부 손님 화면이 조용히 깨지던 것들입니다.
  사진은 목록과 대조만 하지 않고 **실제로 불러 봅니다** (새로 넣은
  사진도 통과해야 하므로).
- ⚠️ **경고와 오류를 섞지 마세요.** 예전에 `e.push(글, "warn")` 으로
  넣었는데 `push` 는 인자를 둘 다 배열에 넣습니다 — 경고가 오류로
  세어져 **내보내기가 통째로 막혔습니다.** `validate()` 는
  `{errors, warns}` 를 돌려줍니다.
- ⚠️ **다시 그린 뒤에 확인하세요.** 사진 확인 표시를 `paintAdmin()`
  앞에서 찍었더니 다시 그리기가 지워 버렸습니다 (`setTimeout(...,0)`).
### 잠금 (js/admin-gate.js)

비밀번호를 물어보게 해 두었습니다. 다만 **진짜 자물쇠가 아닙니다** —
정적 사이트라 확인이 전부 브라우저에서 일어나고, 소스를 보거나 이
파일을 지우면 그냥 열립니다. 막아 주는 것은 *주소를 우연히 아는 사람이
그냥 들어오는 것* 까지입니다.

- ⚠️ **비밀번호를 평문으로 적지 마세요.** 이 저장소는 공개라, 평문을
  적으면 그 비밀번호가 GitHub 에 그대로 올라갑니다. 소금을 붙여
  SHA-256 한 값만 둡니다. 해시라서 안전해지는 것은 아니지만, 적어도
  **글자 자체가 인터넷에 올라가지는 않습니다** — 같은 비밀번호를 다른
  곳에도 쓴다면 이 차이가 큽니다.
- 바꾸는 법:
  `node -e "const c=require('crypto');console.log(c.createHash('sha256').update('aboutmeat-admin-v1:'+'새비밀번호').digest('hex'))"`
  → `PASS_HASH` 에 넣습니다.
- `crypto.subtle` 은 https 에서만 되므로 SHA-256 을 직접 구현해
  두었습니다 (파일을 그냥 열어 확인할 때도 돌아야 합니다).
  표준 구현과 값이 같은 것을 확인했습니다.
- **sessionStorage** 에 풀린 표시를 둡니다. localStorage 로 하면 공용
  PC 에서 영영 열린 채로 남습니다.
- ⚠️ `admin-ui.js` 가 **스스로 시작하지 않습니다.** 잠금이 풀린 뒤
  `bootAdmin()` 을 부릅니다. 거기서 자동 시작하면 잠겨 있어도 내용이
  그려집니다 — 숨기기만 하면 소스에서 다 보입니다.
- **진짜로 막으려면 서버 쪽에서 막아야 합니다.** Vercel 의
  Deployment Protection 을 `/admin.html` 에 거세요.

## 아직 안 된 것

| 항목 | 어디 |
|---|---|
| **사업자 정보** (판매 시작 전 필수 — 전자상거래법 제10조) | `admin.html` 의 사업자 정보 탭 |
| 실제 상품·가격 | `admin.html` 에서 넣고 `products.js` 를 내보내세요 |
| 로그인·회원가입 | `js/app.js` 의 `doLogin` · `doSignup` |
| 결제 | `js/app.js` 의 `checkout` |
| 대량견적 접수 | `js/pages/misc.js` 의 `submitQuote` |
| 아이콘(`icon-*.png` · `apple-touch-icon.png`) | 아직 예전 사이트 마크입니다 |
| 부위 사진 29장 (48개 중 19개만 있음) | `img/e-<slug>.png` 로 넣으면 `encyclopedia.js` 에 `img` 한 줄만 추가 |
