# ABOUTMEAT (aboutmeat.co.kr) — 작업 규칙

**고기 사업자의 문제 해결 플랫폼.** 빌드 도구 없는 정적 HTML/CSS/JS +
Vercel 함수. `index.html` 을 그냥 열면 바로 돕니다.

손님은 고깃집·정육점 사장님과 예비창업자입니다. 이 사이트가 답해야
하는 질문은 하나입니다 — **"고기 장사를 하면서 문제가 생겼을 때 어디에
물어봐야 하는가."**

```
문제 입력 → 이해 → 해결방법 → 업체 찾기 → 견적 비교 → 상담·계약 → 관리
```

모든 디자인·개발 결정은 이 흐름을 기준으로 판단합니다.

⚠️ **가운데 두 칸(이해 · 해결방법)이 한동안 비어 있었습니다.** 고민
분류 열두 개는 SOS 폼에 이름표만 찍는 라벨이었고, 누르면 곧장 "업체
찾기" 로 건너뛰었습니다 — 그러면 지시서 3번이 금지한 **전화번호부**
입니다. 지금은 `/problem/<key>` 가 그 자리를 채웁니다.

> **이 주소의 역사** — 세 번째 서비스입니다.
> ① 축산 요청·견적 매칭 "고리" (Supabase 백엔드 · 소스 48개 · SQL 10개).
> ② 소·돼지 부산물 전문 온라인몰 (2026-09-22 ~ 09-25).
> ③ 지금 — 고기 사업자 문제해결 플랫폼 (2026-09-25 ~).
>
> ⚠️ **지웠지 없앤 게 아닙니다.** 고리는 `git show 93336cb^:src/…`,
> 부산물몰은 `git show 07f5947^:js/…` 로 꺼낼 수 있습니다. 고리의
> Supabase 스키마(`db/*.sql`)는 새 Entity 와 상당 부분 겹쳐서, 백엔드를
> 붙일 때 다시 볼 값어치가 있습니다.

## 구조

```
index.html              SPA 껍데기 (스크립트 순서가 곧 의존 순서)
build-pages.js          주소마다 HTML 파일 + sitemap 을 만듭니다
check.js                전수 점검 (playwright 필요)
css/tokens.css          색·글씨·간격·모서리 — 날값은 여기 한 곳에만
css/app.css             공통 (헤더·버튼·푸터·아래 네비·알림)
css/pages.css           화면별 (히어로·상황선택·띠·폼)
js/data/korean.js       조사(은/는·이/가·을/를) 고르기
js/data/site.js         사업자 정보 · 접수 준비 여부 (WOW_BIZ)
js/data/situations.js   상황 다섯 (창업·운영·문제·확장·정리)
js/data/problems.js     문제 분류 12개 · 히어로 입력창 예시
js/data/services.js     파트너 카테고리 (매칭의 단위)
js/data/startup.js      창업 20단계 · 창업비 계산기가 묻는 것
js/data/photos.js       사진 자리 (지금 비었음) · photoBox · hasPhoto
js/data/check.js        무료 사업진단 8항목 · 점수 구간
js/data/reqforms.js     서비스별 요청서 (덕트는 화구 수, 육류는 월 사용량)
js/data/regions.js      지역 시·도 17개 — 매칭 조건
js/data/posts.js        사장님 연구소 글 12편 · 분류 6개
js/data/guides.js       문제별 해결 가이드 10편 — **흐름의 가운데 두 칸**
js/data/faq.js          자주 묻는 것 — 화면과 구조화 데이터가 같이 읽습니다
js/data/legal-*.js      약관·방침 본문 — 중개·매칭 서비스 기준
js/components/          base(esc·아이콘·알림) · chrome(헤더·푸터·네비)
js/pages/               home · sos · check · start · request · partner
                        lab · problem · quotes · my · search · tools · legal
js/app.js               라우터 · 메타
api/quote.js            SOS·견적 접수 (Vercel 함수)
api/_send.js            보내기 (웹훅 또는 이메일) · 출처 확인
middleware.js           /admin 을 서버에서 막습니다
img/                    사진 자리에 들어가는 그림 41장 (SVG)
img/README.md           자리 41군데 — 파일 이름 · 크기 · 무엇이 찍혀야 하는가
tools/make-img.js       그림을 다시 그립니다 · tools/og.html  공유 이미지
tools/shot.js           화면을 찍어 봅니다 (디자인을 바꿨으면 반드시)
404.html · robots.txt · sitemap.xml · manifest.json · og.jpg · sw.js
```

## 절대 규칙

1. **숫자·후기·실적을 지어내지 않는다.** 업체 수 · 계약 수 · 절감금액 ·
   고객 수 · 평점 · 성공사례 — 실제 데이터가 없으면 **그 구간을 아예
   내지 않습니다.** 메인의 "실제 요청 현황 · 사장님 스토리 · 연구소" 가
   그렇게 되어 있습니다 (`WOW_LIVE` · `WOW_STORIES` · `WOW_POSTS` 가
   비면 함수가 `""` 를 돌려줍니다). `0` 도 숫자입니다.

   ⚠️ **시안에 있던 "3,200+ 사장님 · 1,500+ 검증된 업체 · 만족도 98%",
   업체 카드의 별점 "4.9 (120)", 사례의 "총 창업비 1억 1,800만원 ·
   준비기간 103일" 은 넣지 않았습니다.** 지금 그 값이 하나도 없습니다.
   실제 값이 생기면 그때 넣습니다 — `check.js` 가 이 다섯 꼴을 전부
   잡습니다 (`n+` 꼴 · 만족도 · 누적 · 별점 · 실적 수식어).

   ⚠️ **메인의 원가율은 지어낸 숫자가 아닙니다.** 손님이 방금 적은 월
   매출과 육류 매입비를 나눈 것이라 틀릴 수가 없습니다. 대신 **"몇 %면
   좋다/나쁘다" 고 단정하지 않습니다** — 업종·부위·지역마다 다르고 그
   기준을 뒷받침할 데이터가 우리에게 없습니다. 그래서 상태색
   (`--ok`/`--warn`/`--bad`)도 거기에는 쓰지 않습니다.
2. **값이 없는 항목은 자리표시자를 찍지 말고 줄째 뺀다.** 푸터 사업자
   정보가 그렇습니다. 손님 눈에 `(미기재)` · `undefined` 가 보이면 그
   순간 미완성 사이트로 읽힙니다.
3. **운영자에게 할 말을 손님 화면에 찍지 않는다.** 손님에게는 "지금은
   이곳에서 받지 못합니다" 까지만, 무엇을 설정해야 하는지는
   `console.warn` 으로.
4. **사용자가 쓴 글은 반드시 `esc()` 를 통과시킨다.** SOS 입력창에 적은
   문제를 그대로 찍는 자리가 여럿입니다. 안 쓰면 거기 넣은
   `<img onerror=...>` 가 남의 브라우저에서 돕니다.
5. **하지 않은 일을 했다고 말하지 않는다.** AI 자동분류를 안 하므로
   "분석했습니다" 라고 하지 않습니다. 지킬 수 없으므로 "몇 시간 안에
   연락드립니다" 라고 하지 않습니다.

## 절대 만들면 안 되는 사이트 (지시서 3번)

축산업 전화번호부 · 업체 목록 사이트 · 고기 쇼핑몰 · 미트박스 유사 ·
단순 견적사이트 · 숨고 복제품 · 축산 뉴스 포털 · 구인구직 사이트 ·
복잡한 B2B 사이트.

⚠️ **첫 화면에 업체나 카테고리를 나열하지 마세요.** 손님이 먼저 하는
일은 업체 검색이 아니라 **"내가 지금 무엇 때문에 들어왔는가"** 를 고르는
것입니다. 히어로에서 제일 큰 것이 **입력창**인 이유입니다.

## 색을 어디에 쓸지

| 토큰 | 쓰는 곳 |
|---|---|
| `--burgundy` `#6B2436` | 브랜드. 버튼 · 로고 · 어두운 띠 · 강조 숫자 |
| `--bg` `#FAF8F6` 오프화이트 | 전체 바탕. `--bg-warm` 과 번갈아 칠합니다 |
| `--surface` `#FFFFFF` | 카드만. 화면 전체가 순백이면 차갑고 싸 보입니다 |
| `--red` `#C1272D` | **딱 세 자리.** 히어로 제목의 "고민" · 히어로 입력창의 동그란 보내기 단추 · 모바일 아래 네비의 SOS |
| `--dark` `#201C1B` | 어두운 구간 (히어로 · 창업 프로젝트 · 마지막 CTA). 사진이 들어오면 그 위에 까는 겹 |
| `--ok` `--warn` `--bad` | **상태값에만** (진단 결과 · 배지) |
| `--tint` `--tint-2` | 채도를 거의 없앤 구간 배경 (`.sec-tone` · `.sec-tint`) |
| `--bg-warm` `#F4F5F7` | **카드 면.** 흰 바탕 위 카드는 그림자 대신 이 면으로 구분합니다 |

⚠️ **`--red` 를 네 번째 자리에 쓰지 마세요.** 딥 버건디가 아니라 밝은
빨강을 둔 이유는 하나뿐입니다 — 어두운 바탕이나 38px짜리 동그라미에서
버건디가 검게 보여 다른 칸과 구분이 안 됩니다. 그 조건이 아닌 곳에 쓰면
지시서 30번이 금지한 "지나치게 붉은 정육점 디자인" 이 됩니다.

⚠️ **어두운 구간을 버건디로 칠하지 마세요.** 히어로와 창업 띠는
차콜(`--dark`)입니다. 거기까지 브랜드색으로 칠하면 첫 화면의 절반이
버건디가 됩니다.

⚠️ **버건디 면적을 넓히지 마세요.** 이 색은 "고기" 가 아니라 "전문
서비스" 로 읽히라고 고른 것입니다. 구간 배경이나 카드까지 넓히는 순간
정육점 간판이 됩니다. 고기 산업의 전문성은 **사진과 콘텐츠**에서
나옵니다 — UI 자체는 깔끔하게 둡니다.

⚠️ 금지: 소·돼지 일러스트 남발 · 과도한 그라디언트 · 네온색 ·
AI 스타트업 느낌 · 지나친 아이콘 사용. 아이콘을 늘리기 전에 **"이 자리에
아이콘이 없으면 못 알아보는가"** 를 먼저 물으세요. 대부분은 글자가
낫습니다.

## 글씨와 누르는 것

- **12px 미만 금지.** 제일 작은 단계가 `--t-cap` 12.5px 입니다.
- **누르는 것은 40px 이상** (`--tap` 은 48px). `<button>` 만이 아니라
  `onclick` 을 단 것도 전부 해당합니다. 푸터 링크를 38px 로 두었다가
  전수 점검에 **168건**이 걸렸습니다 — 화면마다 반복되기 때문입니다.
- ⚠️ **`padding` 만으로 높이를 만들지 마세요.** 짧은 이름에서 다시
  낮아집니다. `min-height` + `flex` 로 잡으세요.
- ⚠️ **문장 한가운데 링크를 박지 마세요.** 높이가 16px 이라 누르기
  어렵습니다. 문단 끝에 버튼으로 내놓으세요.

## 한글은 아무 데서나 잘립니다

브라우저 기본값은 **한글을 글자 단위로 끊습니다** ("무료입니 / 다.").
`body` 에 `word-break:keep-all` — 띄어쓰기에서만 끊습니다. 안전판으로
`overflow-wrap:anywhere` 를 같이 둡니다. `keep-all` 만 두면 띄어쓰기
없이 긴 것(주소·계좌번호)이 칸 밖으로 넘쳐 가로 스크롤이 생깁니다.

### `<br class="br-m">` 뒤에는 띄어쓰기를 하나 두세요

넓은 화면에서만 줄을 바꾸려고 `.br-m` 을 씁니다. 좁아지면 이 `<br>` 은
`display:none` 으로 **사라지는데, 띄어쓰기가 없으면 앞뒤 낱말이 그대로
붙습니다** — "고깃집 하나 차리는 데알아볼 게 너무 많으니까."

- ⚠️ 띄어쓰기는 `<br>` **앞이 아니라 뒤**입니다. 줄바꿈 바로 다음에
  오는 공백은 브라우저가 지우므로 넓은 화면에서는 아무 표도 안 납니다.
  앞에 두면 넓은 화면에서 줄 끝에 쓸데없는 공백이 남습니다.
- 이건 **에러도 가로 스크롤도 안 나고 화면도 멀쩡합니다.** 글자만
  붙습니다. `check.js` 의 "줄바꿈 사라져 낱말 붙음" 이 잡습니다.

### 낱말 잘림 검사의 두 가지 사각지대

`check.js` 가 "줄 수 > 낱말 수" 로 잡습니다. 여기서 두 번 틀렸습니다.

- **가운뎃점·빗금·쉼표도 낱말 경계입니다.** "육절기·골절기·진공기" 가
  `·` 에서 줄이 바뀌는 것은 쪼개진 게 아닙니다. 띄어쓰기만 세면 이런
  것이 전부 오탐이 되고, 정작 잡아야 할 "고소 / 함" 이 묻힙니다.
- ⚠️ **가운데 정렬된 글에는 "줄 중간에서 시작" 판단을 쓰면 안 됩니다.**
  가운데 정렬은 줄마다 왼쪽 끝이 달라서, 둘째 줄이 조금만 길어도
  "중간에서 시작했다" 로 잘못 읽습니다. 실제로 쪼개진 낱말을
  1440·1024px 에서 놓치고 390px 에서만 잡았습니다.

## "곱창을(를)" 이라고 쓰지 마세요

조사를 괄호로 때우면 **구글 검색 결과 줄에 그대로 나갑니다.**
`josa(낱말, "은는")` 를 씁니다 (`js/data/korean.js`).

- ⚠️ 이 파일은 `js/data/` 에 있어야 합니다. `build-pages.js` 는
  `js/data/*.js` 와 `app.js` 의 `routeInfo()` 만 읽습니다.
- ⚠️ **"와/과" 만 순서가 거꾸로입니다.** `KO_PAIR` 가 `"와과"` ·
  `"과와"` 둘 다 같은 답을 내도록 받아 둡니다.
- `check.js` 가 `은(는)` · `을(를)` 꼴을 개발자 말로 잡습니다.

## 알림(토스트)은 보이고 들려야 합니다

담기·보냄·동의 누락 안내가 전부 토스트로 나갑니다.

- **모바일에서 아래 네비에 가립니다.** `bottom:calc(var(--mnav-h) + 16px)`.
  ⚠️ 이 미디어쿼리는 `.mnav` 가 나타나는 폭과 **같아야** 합니다.
- **`role="status"` + `aria-live="polite"`.** 없으면 눈이 불편한 손님은
  무슨 일이 일어났는지 알 방법이 없습니다. `assertive` 는 읽던 말을
  자릅니다 — 그렇게까지 급하지 않습니다.

## 눌러도 아무것도 없는 칸을 두지 마세요

- 아직 안 만든 화면은 **빈 화면으로 두지 않습니다.** 무엇을 할 곳인지
  적고 지금 할 수 있는 것(SOS·전화)을 줍니다 (`app.js` 의 `SOON`).
  전부 `noindex` 라 검색에는 안 올라갑니다.
- 화면을 실제로 만들면 `SOON` 에서 빼세요. 그때 sitemap 에 들어갑니다.
- **전화번호가 없으면 전화 버튼이 안 나옵니다.** 눌렀는데 안 걸리는
  것보다 없는 게 낫습니다 (`CallButton`).

## 접수는 동의부터

- ⚠️ **동의 확인 코드를 지우지 마세요.** 화면에서 한 번, 서버
  (`api/quote.js` 의 `agree !== true`)에서 한 번 막습니다. 화면만 믿으면
  그 주소로 직접 보내는 것을 못 막습니다.
- **수집 항목 · 이용 목적 · 보유 기간을 동의를 받는 자리에서 같이**
  보여 줍니다 (개인정보보호법 제15조 제2항). 방침 링크만 두고 넘어가지
  않습니다.
- ⚠️ **"접수하지 못한다" 는 말을 폼 아래가 아니라 위에 둡니다.** 다
  적고 누른 뒤에 알리면 바쁜 사장님의 시간을 버리게 하는 짓입니다.
- ⚠️ **`WOW_BIZ.sosReady` 는 "환경변수 설정을 마쳤다" 는 표시**입니다.
  여기에 뭘 적는다고 접수가 되는 게 아닙니다. 설정 전에 켜 두면 손님이
  다 적고 눌렀는데 실패합니다.

### 접수가 실제로 되게 하려면

`api/quote.js` 는 **저장하지 않습니다.** 서버도 DB 도 없으니 받아서 밖으로
보내기만 합니다. Vercel → Settings → Environment Variables 에 넣으세요.

**하나만 넣으면 전부 그리로 옵니다.**

| 변수 | 무엇 |
|---|---|
| `INTAKE_WEBHOOK_URL` | 접수 JSON 을 그대로 POST. 슬랙 워크플로 주소만 넣어도 채널에 바로 뜹니다 |
| `RESEND_API_KEY` + `INTAKE_EMAIL_TO` | 메일로 받기 (resend.com). 주소는 쉼표로 여러 개 |
| `INTAKE_EMAIL_FROM` | 보내는 주소 (기본값 `onboarding@resend.dev`) |

종류별로 나눠 받고 싶을 때만 더 넣습니다 (없으면 위를 씁니다) —
`SOS_WEBHOOK_URL`·`SOS_EMAIL_TO` · `QUOTE_WEBHOOK_URL`·`QUOTE_EMAIL_TO` ·
`PARTNER_WEBHOOK_URL`·`PARTNER_EMAIL_TO`.

⚠️ 예전 이름 `ORDER_WEBHOOK_URL`·`ORDER_EMAIL_TO`·`ORDER_EMAIL_FROM` 도
그대로 받습니다 (부산물몰 때 이름입니다). 새로 넣을 때는 `INTAKE_*`.

⚠️ **환경변수를 넣은 뒤에는 다시 배포해야 적용됩니다.** Vercel 은 이미
떠 있는 함수에 값을 밀어 넣지 않습니다. Deployments → 맨 위 → Redeploy.

⚠️ 세 환경(Production·Preview·Development) 중 **Production 에 체크**가
되어 있는지 보세요. 미리보기에서만 되는 일이 생깁니다.

⚠️ 하나도 없으면 **503 을 돌려줍니다.** 받을 곳이 없는데 "접수되었습니다"
라고 하면 손님은 기다리고 요청은 사라집니다.

⚠️ 넣었으면 `js/data/site.js` 의 `WOW_BIZ.sosReady` 를 `true` 로 바꾸고
커밋하세요. 그 전까지는 폼 **위에** "지금은 이 양식으로 접수하지 못합니다"
가 뜹니다. 반대로 **설정 전에 켜 두면 손님이 다 적고 눌렀는데 실패합니다.**

> **지금 상태 (2026-09-26): 환경변수를 아직 넣지 않아 `sosReady` 는
> `false` 입니다.** 켜는 순서는 `js/data/site.js` 의 그 자리에 다섯
> 단계로 적어 두었습니다 — 순서를 바꾸지 마세요.

⚠️ **"못 받습니다" 로 끝내면 막다른 길입니다.** 전화번호도 아직 없어서
지금은 연락할 방법이 하나도 없습니다. 그러면 손님은 그냥 나갑니다.
그래서 안내에 **지금 당장 되는 것**(진단 · 창업비 정리표 · 견적 비교 ·
연구소)을 같이 냅니다 — 가입 없이, 사람 손을 안 타고 되는 것들입니다.
`check.js` 가 "안내가 막다른 길이 아니다" 로 지킵니다.

⚠️ **안 쓰는 회사를 방침에 적어 두는 것도 사실과 다른 방침입니다.**
한때 접수처를 슬랙으로 적어 뒀다가 뺐습니다 — 환경변수를 안 넣었으니
슬랙으로 가는 것이 하나도 없기 때문입니다. `check.js` 가 "접수처가
없는데 방침에 전달·발송 수탁자가 적혀 있으면" 잡습니다.
환경변수를 채우는 **바로 그날** 다시 넣으세요.

⚠️ **`api/` 에서 밑줄로 시작하는 파일**(`_send.js`)은 주소가 되지
않습니다. Vercel 규칙입니다 — 이름을 바꾸면 `/api/_send` 가 열립니다.

⚠️ **키를 저장소에 적지 마세요.** 공개 저장소입니다.

⚠️ **개인정보 처리위탁이 늘어납니다.** 이 환경변수를 채우는 순간 그
회사(슬랙·Resend 등)가 손님의 성함·연락처를 받게 됩니다.
`js/data/legal-privacy.js` 의 `trustees` 에 한 줄을 **같은 날** 넣으세요
(제26조). 미국 회사면 국가도 적습니다 (제28조의8).

### 화면이 보내는 칸과 서버가 읽는 칸이 어긋나면 조용히 사라집니다

⚠️ 실제로 그랬습니다 — 견적 요청이 `service`·`budget`·`detail` 을
보내는데 `api/quote.js` 는 `svc` 하나만 읽고 있었고, 파트너 등록은
아예 견적으로 처리되어 **업체명과 취급 서비스가 통째로 빠졌습니다.**
에러도 안 나고 접수도 성공합니다. 받아 보는 사람만 이상해집니다.

```bash
node tools/test-api.js   # 화면이 보낸 칸이 빠짐없이 나가는지 (23개)
```

⚠️ `node check.js` 는 이걸 **못 잡습니다.** 그쪽은 브라우저로 화면만
보고 Vercel 함수는 돌리지 않습니다. 화면의 `body = {...}` 를 고치면
`api/quote.js` 와 이 검사를 **같이** 고치세요.

### 화면 밖에서 오는 요청은 받지 않습니다

`api/_send.js` 의 `fromOurPages(req)` 가 **Origin 헤더**를 봅니다.
브라우저는 POST 에 Origin 을 반드시 붙이므로, 없거나 우리 host 와 다르면
화면에서 온 것이 아닙니다. 정해 둔 도메인이 아니라 **실제로 들어온
host** 와 비교하므로 www 든 미리보기 주소든 알아서 맞습니다.

⚠️ **자물쇠가 아니라 문턱입니다.** 작정하고 도배하는 것은 **Vercel 의
Firewall → Rate Limit** 으로 막으세요 — 코드로는 셀 곳이 없습니다
(함수가 요청마다 새로 뜨고 기억을 공유하지 않습니다).

## 주소는 진짜 경로입니다 (해시가 아닙니다)

```
node build-pages.js      # 주소마다 HTML + sitemap.xml 을 만듭니다
                         # (없어진 주소의 파일도 같이 치웁니다)
```

- ⚠️ **`location.hash` 를 읽지 마세요.** 늘 빈 문자열이라 **에러 없이
  조용히 틀린 답**을 돌려줍니다. 화면은 그려지니 전수 점검도 통과합니다.
  `nowPath()` · `nowQS(k)` 를 쓰세요.
- 자산 경로는 반드시 절대 경로(`/js/…`)여야 합니다. 상대 경로면
  `/start/cost` 에서 `/start/js/…` 를 찾아 스크립트가 통째로 404 입니다.
- 링크는 `href="/sos"` 로 씁니다. click 가로채기가 새로고침 없이
  넘깁니다. 새 탭·다운로드·외부 링크·`/api` 는 안 건드립니다.

## 화면을 새로 만들 때

`js/app.js` 한 곳에서 다 합니다.
1. `META` 에 제목과 **설명**을 넣습니다. 설명을 비우면 구글이 본문에서
   아무 문장이나 가져다 씁니다.
2. `SOON` 에서 뺍니다.
3. `routeInfo()` 에 어떤 화면인지 추가합니다.
4. `render()` 의 switch 에 그리는 함수를 답니다.
5. `build-pages.js` 의 `allRoutes()` 에 주소를 넣어야 HTML 파일이
   만들어집니다.

사람마다 내용이 다른 화면(MY·로그인)과 결과 화면은 `NOINDEX` 에
넣으세요.

⚠️ `build-pages.js` 가 **겹치는 제목·설명이 있으면 멈춥니다.** 구글
서치콘솔이 "중복" 으로 표시하고 검색 결과 두 줄이 같은 말을 합니다.

## `vercel.json` 에 주석을 넣지 마세요

JSON 에는 주석이 없어서 `"//"` 키로 설명을 넣었더니 **Vercel 이
"Invalid vercel.json" 으로 배포를 거부합니다.** 헤더 항목에는 `key` 와
`value` 말고 아무것도 못 넣습니다. 설명은 여기(CLAUDE.md)에 적으세요.
`build-pages.js` 가 만들기 전에 검사하고 멈춥니다.

⚠️ CSP 의 `script-src` 에 `'unsafe-inline'` 이 있는 것은 화면 곳곳이
`onclick=` 이기 때문입니다. **빼면 사이트가 통째로 안 돕니다.** 글꼴
CDN 을 바꾸면 `style-src` · `font-src` 도 같이 고치세요. ⚠️ CSP 를
고치면 **적어만 두지 말고 실제로 띄워 보세요.**

⚠️ HSTS 에 `preload` 는 일부러 안 붙였습니다 — 붙이면 브라우저에 박혀
되돌리는 데 몇 달이 걸립니다.

## 화면의 짜임새는 한 벌입니다

> **2026-09-26 — 아정당(ajd.co.kr)을 기준으로 다시 올렸습니다.**
> 밝고 넓은 여백 · 테두리 없는 카드 · 큼직한 아이콘 타일이 핵심입니다.

**머리(hero)는 밝은 두 칸입니다** — 글 왼쪽, 그림 오른쪽(`.pgh`).
메인은 `.hero-lt`, 나머지 화면은 `.pg-hero` + `.pgh`. **모든 화면이
같습니다.** 하나만 다르게 두지 마세요.

⚠️ **그림을 배경으로 깔지 마세요.** 예전에는 차콜 바탕에 그림을 깔고
어두운 겹을 얹었습니다. 실제 현장 사진이라면 그게 맞지만, 지금 들어
있는 것은 코드로 그린 벡터 그림이라 **글자 밑에서 흐릿한 갈색
덩어리**로만 보였습니다. 실제 사진이 들어오면 이 결정을 다시 보세요.

⚠️ 어두운 바탕용으로 둔 것을 밝은 머리에 그대로 쓰지 마세요.
`btn-w` · `btn-gh` · 반투명 흰색 상자(`.pg-now`)는 **밝은 바탕에서
아무것도 안 보입니다.** `.pg-hero-d` 쪽 규칙은 그대로 남겨 두었습니다.

**카드는 테두리가 없습니다.** 칸마다 1px 선을 두르면 표처럼 읽히고,
선이 없으면 카드가 떠 있는 것처럼 읽힙니다 — 같은 내용이라도 뒤쪽이
훨씬 정돈돼 보입니다. 대신 아주 옅은 그림자와 큰 모서리(`--r-xl`).

⚠️ **적는 줄은 카드가 아닙니다.** 창업비·계산기의 입력 줄(`.tl-i` ·
`.cost-i` · `.svc-i`)은 옅은 면으로 **깔려** 있어야 합니다. 떠 있으면
몇 줄만 이어져도 눈이 아픕니다.

**아이콘은 늘리지 않고 또렷하게.** 둥근 사각 타일 54px 한 벌
(`.cat-ic` · `.tl-c-ic` · `.mycard-ic` · `.prob-ic` · `.svc-g-ic`).
개수를 늘리는 것과 크게 그리는 것은 다릅니다 — 지시서 30번이 금지한
것은 **개수**입니다.

**숫자는 폭을 고정합니다** (`font-variant-numeric:tabular-nums`).
기본 글꼴은 1 이 좁아서 금액이 바뀔 때마다 칸이 들썩입니다. 숫자를
크게 보여 주는 사이트에서 제일 티 나는 허술함입니다.

## 장식을 얹었다가 걷어냈습니다 — 꼭 읽으세요

> **2026-09-26 — 한 번 해 보고 되돌린 기록입니다.** 같은 길을 다시
> 가지 마세요.

"밋밋하다" 는 말을 듣고 **금빛 실선 · 눌러 찍은 듯한 그라디언트 타일 ·
두 겹 그림자 · 그림 전체를 덮는 명암막**을 넣었습니다. 결과는
고급스러움이 아니라 **꾸민 티**였고, 지시서 30번이 금지한 "AI 스타트업
느낌" 에 가까웠습니다.

**걷어낸 것 (되살리지 마세요)**

| 걷어낸 것 | 왜 |
|---|---|
| 금빛 실선(머리말·로고·패널·칩 테두리) | 화면 어디에도 그런 장식이 없어야 정돈돼 보입니다 |
| 아이콘 타일의 그라디언트 + 안쪽 빛·그늘 | 눌러 찍은 듯한 가짜 입체. 납작한 한 면이 낫습니다 |
| 카드 두 겹 그림자 | 카드가 뜨면 화면이 무거워집니다 |
| 그림 카드 뒤의 빛 · 버튼 안쪽 윗선 | 같은 이유 |
| 그림 전체를 덮던 빛·그늘·모서리 눌림 | **사진에 필터 씌운 것** 처럼 인위적이었습니다 |

**대신 지키는 것**

- **카드는 그림자가 아니라 면으로 구분합니다.** 흰 구간에서는 옅은
  회색 면(`--bg-warm`), 회색·색 구간에서는 흰 면. 띄우지 않습니다.
- **바탕은 흰색**(`--bg: #FFFFFF`)이고 글자는 **중성 회색**입니다.
  웜 그레이는 베이지 바탕에서만 맞습니다 — 흰 바탕에 두면 누렇게 뜹니다.
- **그림의 색은 세 가지뿐**입니다 — 고기(`meat`) · 브랜드(`burg`) ·
  나무(`wood`). 나머지는 전부 회색 단계입니다. 늘리면 다시 탁해집니다.
- 입체감이 필요하면 **조각마다** 넣습니다 (쇼케이스 유리의 빛줄,
  문짝의 명암, 상판의 두께). 화면 전체를 덮지 않습니다.

⚠️ 다시 "밋밋하다" 는 말이 나오면 **장식을 더하지 말고** 여백 · 글자
크기 차이 · 색 있는 요소의 **자리**를 먼저 보세요. 기준 화면(아정당)이
화려해 보이는 것은 장식이 많아서가 아니라 **색 있는 것이 놓일 자리에
놓여 있어서**입니다.

## 클래스 이름이 겹치면 조용히 깨집니다

⚠️ **`.lg` 가 헤더 로고와 약관 본문 두 곳에 있었습니다.** 약관 쪽
`padding:var(--s8) 0 var(--s10)` 이 로고에 그대로 먹어서, **모든
화면에서 로고 윗부분이 잘려** 있었습니다. 에러도 가로 스크롤도 안 나고
전수 점검도 계속 통과했습니다 — 화면을 **실제로 찍어 보고서야**
찾았습니다 (지금은 `.lgp`).

```bash
node tools/shot.js "/|1440|home" "/|390|home-m"   # /tmp/shots/*.png
```

⚠️ **디자인을 바꿨으면 적어만 두지 말고 찍어 보세요.** `check.js` 는
숫자로 잴 수 있는 것(글씨 크기 · 누름 크기 · 넘침 · 칸 섞임)만 봅니다.
"보기 흉한가" 는 못 봅니다.

## 사진

⚠️ **AI 티가 강한 고기 사진을 남발하지 마세요** (지시서 32번). 이 사이트는
고기 파는 곳이 아니라 **사업자를 위한 서비스**입니다. 실제 고깃집 ·
정육점 · 주방 · 덕트 · 쇼케이스 · 사장 · 작업현장 사진을 씁니다.
**사람과 사업장 사진 비중을 높이세요.**

⚠️ **글자가 박힌 이미지를 쓰지 마세요.** AI 가 만든 한글은 글자가
틀려 있습니다.

### 지금 들어 있는 것은 사진이 아니라 그림입니다

> **사장님 결정 (2026-09-26): 실제 사진이 생기기 전까지는 그림 그대로
> 갑니다.** 그림을 사진처럼 보이게 고치거나, 임시라는 이유로 빼지
> 마세요. 사진이 준비되면 그때 `WOW_PHOTOS` 의 경로만 바꿉니다.

⚠️ **실제 사진이 한 장도 없습니다.** `img/` 의 그림은 코드로 그린
벡터 그림(SVG)입니다. 빈 액자보다는 낫지만, **실제 고깃집 · 정육점 ·
주방 · 덕트 · 쇼케이스 · 사장님 사진으로 바꾸는 것이 원래 계획**입니다
(지시서 32번). 그림은 "여기가 어떤 자리인지" 만 알려 줍니다 — 손님을
설득하는 것은 현장 사진입니다.

```bash
node tools/make-img.js   # img/*.svg 를 다시 그립니다
```

- ⚠️ **`img/` 의 SVG 를 손으로 고치지 마세요.** `tools/img-scenes.js`
  (장면)와 `tools/img-lib.js`(사람·쇼케이스·후드 같은 조각)를 고치고
  다시 돌리세요. `make-img.js` 가 photos.js 가 찾는 것과 실제로 그린
  것이 어긋나면 알려 줍니다.
- ⚠️ **얼굴을 그리지 않습니다.** 얼굴을 그리는 순간 "누구" 가 되고,
  그러면 우리 손님이 아닌 사람이 됩니다.
- ⚠️ **그리는 순서가 곧 앞뒤입니다.** 카운터를 사람보다 먼저 그리면
  사람이 카운터 **위에 올라선 것처럼** 보입니다. 실제로 그랬습니다.
- ⚠️ 어두운 면 위에 어두운 사람을 두지 마세요. 한 장이 통째로 안
  보였습니다.
- ⚠️ 그림 안에 **글자를 넣지 않습니다.** 확대도 번역도 안 됩니다.
- ⚠️ 바탕 사진(`hero` · `final`)은 **아주 납작한 띠**로 잘립니다.
  16:9 로 그리면 가운데 30% 만 남습니다. 처음부터 납작하게 그리고,
  `object-position` 으로 자르는 기준을 맞추세요 — 넓은 화면에서는
  아래쪽, 좁은 화면에서는 오른쪽입니다 (사장님이 오른쪽에 있습니다).
- ⚠️ 히어로는 **제목 글자가 왼쪽 60% 를 덮습니다.** 볼 것을 가운데
  두면 글자 뒤에 숨습니다.

`og.jpg` 는 `tools/og.html` 을 브라우저로 띄워 찍습니다.

### 사진이 없을 때는 짜임새 자체가 바뀝니다

`js/data/photos.js` 의 `WOW_PHOTOS` 가 비면 화면이 `hasPhoto(key)` 를
보고 갈라집니다. 지금은 41장이 다 차 있어서 사진 짜임새로 돕니다.

- 사진이 있으면 — 사진 카드 · 왼쪽에 세운 사진 기둥 · 히어로 바탕사진.
- 사진이 없으면 — **빈 액자를 크게 두지 않고** 글이 주인공인 짜임새로
  갑니다 (상황 카드는 아이콘+글, 고민 목록은 세 칸으로 넓게).
- ⚠️ **"이미지 준비 중" 같은 자리표시자를 찍지 마세요** (절대 규칙 2).
  빈 회색 칸이 보이면 그 순간 미완성 사이트로 읽힙니다.
- ⚠️ 사진이 없는데 `alt` 만 단 빈 칸을 만들지 마세요. 읽어 주는
  프로그램이 없는 사진을 설명합니다 — 없는 것보다 나쁩니다.

실제 사진으로 바꿀 때는 파일을 `/img/` 에 넣고 `WOW_PHOTOS` 의 경로만
바꾸면 됩니다 (경로는 **절대 경로**). 자리는 `hero` · `sit-*`(5) ·
`cat-*`(6) · `worry` · `partner` · `band-meat` · `final` · 화면별 히어로 ·
연구소 글 썸네일까지 **모두 41군데**입니다. 자리마다 무엇이 찍혀야
하는지는 `img/README.md` 에 표로 적어 두었습니다.

⚠️ `alt` 는 **사진에 무엇이 찍혀 있는지**를 적습니다. "고깃집 사진"
같은 말은 눈이 불편한 손님에게 아무것도 알려 주지 않습니다.
`check.js` 가 `alt` 가 비었는지 봅니다.

`og.jpg` 는 브라우저로 그립니다(`file://` HTML → 스크린샷). PIL 로
만들었더니 서버에 한글 글꼴이 없어 전부 네모로 나왔습니다 — 띄워 보지
않았으면 그대로 올라갈 뻔했습니다.

## 진단은 판정하지 않습니다

`/check` 는 "사장님 가게가 좋다/나쁘다" 를 매기지 않습니다. 매기려면
업종·평수·지역·부위 구성별 **기준값**이 있어야 하는데 우리에게
없습니다. 없는 기준으로 점수를 내면 그게 지어낸 숫자입니다.

대신 **"지금 무엇을 알고 계시고 무엇을 모르시는지"** 를 셉니다.
사장님 답을 그대로 세는 것이라 지어낼 여지가 없습니다.

- ⚠️ 점수 옆에 **이 점수가 무엇인지** 적습니다. "78점" 만 크게 띄우면
  업계 평균과 견준 점수로 읽힙니다.
- ⚠️ 메인·진단의 **육류원가율은 지어낸 숫자가 아닙니다.** 손님이 적은
  월 매출과 매입비를 나눈 것입니다. 다만 **"몇 %면 좋다" 고 단정하지
  않고** 상태색(`--ok`/`--warn`/`--bad`)도 거기에는 쓰지 않습니다.
- 진단 결과·창업 체크·창업비는 **서버로 안 보냅니다.** 그 화면들에
  그렇게 적혀 있고, `check.js` 가 실제로 그런지 봅니다.

## 창업비는 계산기가 아니라 정리표입니다

`/start/cost` 는 **예상 금액을 알려 주지 않습니다.** 지역·평수·시공
수준에 따라 항목마다 몇 배씩 차이 납니다. 근거 없는 "예상 3,200만원" 을
적으면 사장님이 그 숫자로 돈을 빌리러 갑니다.

하는 일은 세 가지입니다 — 빠뜨리기 쉬운 항목을 전부 늘어놓고, 사장님이
**실제로 받은 견적**을 적으면, 합계와 **아직 안 받은 칸**을 보여 줍니다.
숫자가 전부 사장님 것이라 틀릴 여지가 없습니다.

⚠️ `check.js` 의 "창업비: 안 적으면 숫자를 지어내지 않는다" 가 이걸
지킵니다. 예상 금액을 넣어 보면 걸립니다.

## 업체에 연락처를 넘기는 것은 따로 동의받습니다

요청서에서 받는 동의는 **접수와 상담까지**입니다. 업체에 성함·연락처를
넘기는 순간이 **제3자 제공**이고(개인정보보호법 제17조), 누구에게
넘기는지 정해지기 전에 뭉뚱그려 받는 동의는 제17조 제2항 위반입니다.

- 업체가 정해지면 **상호를 알리고 그때 다시** 받습니다.
- 그래서 SOS·견적 요청의 동의 상자에 "업체에 연락처를 전달하는 것은
  여기에 포함되지 않습니다" 가 적혀 있습니다. **지우지 마세요.**
- 방침 제4조가 같은 말을 합니다. 한쪽만 고치면 둘이 어긋납니다.

## 문제별 해결 가이드 (`/problem/<key>`)

> **2026-09-26 — 사장님 지시: "우리 사이트가 어떠한 사이트인지 다시
> 제대로 생각하고 기능을 추가해라."**

이 사이트가 **전화번호부가 아닌 이유**가 여기 있습니다. 업체를 대 주는
곳은 이미 많습니다. 다른 점은 **업체를 부르기 전에 무엇을 확인하고
무엇을 물어봐야 하는지**를 아는 것이고, 그건 말이 아니라 이 화면으로만
증명됩니다.

화면 순서가 곧 **사장님이 실제로 하실 순서**입니다.

| 칸 | 무엇 |
|---|---|
| `warn` | 다치거나 큰돈이 나가는 신호 — **제일 위** |
| `self` | 지금 5분 안에 직접 확인할 것 |
| `causes` | 먼저 의심할 것 |
| `tell` | 업체에 미리 알려 줄 것 (견적이 정확해집니다) |
| `ask` | **견적 받을 때 물어볼 것** ← 이 사이트의 핵심 자산 |
| `law` | 법·제도 + **어디서 확인하는지** |

- ⚠️ **순서를 바꾸지 마세요.** "물어볼 것" 을 위로 올리면 아직 무슨
  문제인지도 모르는 분께 질문지부터 들이미는 꼴이 됩니다.
- ⚠️ **금액·비율을 적지 마세요.** "덕트 공사는 보통 300만원" ·
  "수율 65%면 정상" 은 부위·지역·업종·시공 수준마다 달라서 우리가 댈
  근거가 없습니다. 사장님이 그 숫자로 업체와 싸우거나 돈을 빌리러
  갑니다. `check.js` 의 "가이드에 지어낸 금액·비율이 없다" 가 `만원` ·
  `억원` · `숫자+%` 를 전부 잡습니다.
- ⚠️ **법령은 바뀝니다.** `law.where` 에 확인하는 곳을 반드시 같이
  적습니다. 비워 두면 검사가 잡습니다.
- ⚠️ **가이드가 없는 분류를 숨기지 마세요.** 숨기면 "여기서는 그건 못
  물어보나" 로 읽힙니다. `/problems` 아래쪽에 "아직 정리 중" 으로 내고,
  누르면 SOS 로 갑니다.
- ⚠️ 고민 카드에서 **곧장 폼이나 업체로 보내지 마세요.** 가이드가 있으면
  가이드로 먼저 갑니다 (`wowHasGuide`). `check.js` 가 두 걸음을 다
  따라가 봅니다.

## 키 컬러는 하나, 분류색은 여덟

> **2026-09-26 — 사장님 지시: "키 컬러가 하나로 통일돼 있어 밋밋하다."**

⚠️ **버건디 면적을 넓혀서 푸는 문제가 아닙니다.** 그러면 지시서 30번이
금지한 정육점 간판이 됩니다. 기준 화면(아정당·숨고)이 화려해 보이는
것은 브랜드색이 넓어서가 아니라 **분류마다 색이 다르기 때문**입니다.

- 색은 **데이터가 정합니다** — `tone:"t3"` (problems · services ·
  situations · posts 의 분류). CSS 에 카테고리 이름을 박아 두면 분류가
  늘 때마다 CSS 를 고쳐야 합니다.
- 화면에서는 `tnClass(tone)` 가 `tn3` 을 붙이고, 그 클래스가
  `--ic-ink` · `--ic-bg` 두 변수를 갈아 끼웁니다.
- ⚠️ 쓰는 곳은 **아이콘 타일 · 배지 · 작은 칩뿐**입니다. 구간 배경 ·
  카드 전체 · 버튼에는 쓰지 마세요. **버튼과 로고와 어두운 띠는 그대로
  버건디입니다 — 브랜드는 하나입니다.**
- ⚠️ **한 줄에 나란히 서는 것끼리는 서로 다른 색**이어야 합니다. 같은
  색이 둘이면 색을 쓴 뜻이 없어집니다 (메인의 고민 여섯 · 카테고리
  여섯 · 상황 다섯 · 연구소 분류 여섯이 각각 전부 다릅니다).
- ⚠️ 되돌림 값(`var(--ic-ink, var(--burgundy))`)을 빼지 마세요. tone 이
  안 붙은 자리는 예전 그대로 버건디여야 합니다.

## 없는 것을 조용히 넘어가지 않게 하는 두 가지 검사

⚠️ 둘 다 **에러가 안 나고 화면도 멀쩡해서** 한참 뒤에야 발견됩니다.
`node build-pages.js` 가 만들기 전에 검사하고 멈춥니다.

### `js/data` 에 있는데 build-pages 가 안 읽는 파일

⚠️ `build-pages.js` 의 데이터 목록은 **glob 이 아니라 손으로 적은
목록**입니다. 새 데이터 파일을 만들고 거기 안 넣으면 그 데이터를 쓰는
주소가 **에러 없이 통째로 안 만들어집니다.** `guides.js` 에서 실제로
그랬습니다 — "페이지 33개" 만 보고 넘어갈 뻔했습니다.
⚠️ 목록의 **순서가 곧 의존 순서**입니다.

### 정의되지 않은 CSS 변수

⚠️ **없는 변수는 에러가 아니라 침묵입니다.** `var(--t-h1)` 처럼 없는
이름을 쓰면 그 줄만 통째로 무시되고, 제목이 본문 크기로 나옵니다.
콘솔에도 안 찍히고 전수 점검도 통과합니다 — 가이드 화면의 `h1` 이
15px 로 나갔습니다. 넣고 보니 **금빛을 걷어낼 때 남은 `--gold` ·
`--gold-on-dark` 세 군데**도 같이 나왔습니다.
되돌림 값이 있는 것(`var(--x, 기본)`)은 일부러 그런 것이라 봐줍니다.

## 업체 명단을 만들지 마세요

`/partners` 는 "업체 찾기" 지만 **명단이 아닙니다.** 무엇이 필요한지
고르는 화면입니다. 파트너가 실제로 등록되어도 이 화면이 명단이 되면
안 됩니다 — 그 순간 지시서 3번이 금지한 전화번호부가 됩니다.

⚠️ 업체 이름 · 평점 · 시공건수를 화면에 만들지 마세요. 실제 파트너가
생기기 전까지는 **지어내는 것**이고, 생긴 뒤에도 평점은 실제 후기가
쌓인 뒤의 일입니다.

## 아이콘

`js/components/base.js` 의 `IC` 가 전부입니다. 선 굵기 1.6, 24×24 격자,
면 없이 선으로만.

- **고를 수 있는 칸**(상황·서비스·진단 항목·창업 단계)과 **상태 표시**에
  씁니다. 같은 모양의 칸이 여러 개 나란히 있을 때 눈이 훑을 수 있게
  하는 것이 목적입니다.
- ⚠️ 목록·표·본문에는 넣지 마세요. 대개 글자가 낫습니다.
- ⚠️ 소·돼지 그림은 넣지 않습니다. 이 사이트는 고기를 파는 곳이 아니라
  **사업자를 위한 서비스**입니다.

## 연구소 글을 쓸 때

이 사이트가 "왜 여기를 써야 하는가" 에 답하는 자리입니다. 업체를 연결해
주는 곳은 많고, 다른 점은 **고기 장사를 안다**는 것인데 그건 말이 아니라
글로만 증명됩니다.

1. ⚠️ **시장 통계를 지어내지 않습니다.** "평균 원가율 35%" 를 쓰려면
   그 숫자를 어디서 세었는지 댈 수 있어야 합니다. 못 대면 안 씁니다.
2. 대신 **법령·제도 사실** · **체크리스트** · **물어볼 질문**으로
   씁니다. 틀릴 여지가 없고, 사장님이 그 자리에서 씁니다.
3. ⚠️ 법령은 바뀝니다. 조문을 적을 때는 **어디서 확인하는지**를 같이
   적습니다 (관할 구청 · 세무서 · 고용노동부).
4. 글 끝은 항상 **지금 할 수 있는 일**로 맺습니다.
5. ⚠️ "저희가 해 드립니다" 를 함부로 쓰지 않습니다. 우리가 하는 일은
   정리 · 매칭 · 견적까지입니다. 고치는 건 업체가 합니다.
6. ⚠️ 본문에서 허용한 표시는 `**굵게**` **하나뿐**입니다. `labMark()` 가
   `esc()` 를 먼저 통과시킨 다음 별표만 되살립니다 — **순서를 바꾸지
   마세요.** 바꾸면 글에 넣은 `<img onerror=…>` 가 돕니다.

## 도구는 사장님 숫자를 나누는 데까지입니다

`/tools` 아래 두 가지를 새로 만들었습니다. 업체를 붙여 주는 곳은 많고,
우리가 다른 점은 **고기 장사의 숫자를 안다**는 것인데 그건 말로는
증명이 안 됩니다. 써 보게 해야 합니다.

| 주소 | 무엇 | 답이 나오는 근거 |
|---|---|---|
| `/tools/yield` | 수율 원가 | 매입 단가 × 매입 무게 ÷ 손질 후 무게 |
| `/tools/bep` | 손익분기 | 고정비 ÷ 공헌이익률(100% − 변동비율) |

⚠️ **기준값을 만들지 마세요.** "수율 65%면 정상" · "원가율 35% 이하로
맞추세요" 는 부위·손질 기준·업종·지역마다 달라서 우리가 댈 근거가
없습니다. 그래서 **판정하지 않고 상태색(`--ok`/`--warn`/`--bad`)도
쓰지 않습니다** — 메인·진단의 원가율과 같은 이유입니다.

⚠️ **안 적은 칸은 `—` 입니다.** 0 으로 치거나 평균값으로 메우면 그
순간 지어낸 숫자가 됩니다. 대신 **무엇을 더 적어야 답이 나오는지**를
적어 줍니다. `check.js` 의 "수율: 안 적으면 숫자를 지어내지 않는다" 가
이걸 지킵니다.

⚠️ **많이 팔수록 손해인 조건을 얼버무리지 마세요.** 변동비율 합이
100% 를 넘으면 손익분기 매출이라는 것이 없습니다. 큰 숫자를 하나
내놓는 대신 "이 조건에서는 많이 팔수록 더 손해입니다" 라고 적습니다.

⚠️ 손익분기의 **인건비는 고정비에 넣었습니다 — 월급제 기준**입니다.
매출에 따라 시간이 늘고 주는 가게는 일부가 변동비입니다. 우리가 대신
정해 주면 그게 지어낸 기준이라, 화면에 그 말을 적고 사장님이 고르시게
둡니다.

⚠️ **입력 중에 화면을 다시 그리지 마세요.** 커서가 날아가고 적던 것이
사라집니다. 결과 칸(`#yl-res` · `#bep-res`)만 갈아 끼웁니다 — 창업비
정리표와 같은 방법입니다.

## 견적 비교에서 "제일 좋다" 고 하지 마세요

`/quotes` 는 받은 견적을 나란히 놓고 **제일 낮은 / 제일 짧은 / 제일 긴**
것을 표시합니다. 거기까지입니다.

⚠️ **"이게 제일 좋습니다" 라고 하면 안 됩니다.** 우리는 그 공사를 보지
않았고, 제일 싼 것이 제일 좋은 것도 아닙니다. 포함 범위가 다르면 금액
비교 자체가 뜻이 없습니다 — 그래서 화면이 그 말을 먼저 합니다.

## 이 브라우저에 남는 것들

로그인이 없어도 다시 올 이유를 만드는 장치입니다. 전부 `localStorage`
이고 **서버로 보내지 않습니다.**

| 키 | 무엇 | 어디서 |
|---|---|---|
| `wow.check.v1` | 진단 점수와 약한 항목 | `/check` |
| `wow.start.v1` | 창업 20단계 체크 | `/start` |
| `wow.cost.v1` | 창업비 정리표 금액 | `/start/cost` |
| `wow.quotes.v1` | 견적 비교 내용 · 물어볼 것 체크 | `/quotes` |
| `wow.lab.v1` | 글마다 체크리스트 | `/lab/:slug` |
| `wow.read.v1` | 최근 본 글 주소 20개 | `/lab/:slug` |
| `wow.yield.v1` | 수율 원가 계산에 적은 숫자 | `/tools/yield` |
| `wow.bep.v1` | 손익분기 계산에 적은 숫자 | `/tools/bep` |

- ⚠️ **성함·연락처를 담지 않습니다.** 가게 컴퓨터·태블릿은 여러 사람이
  씁니다. 진단도 **적으신 매출·매입비 숫자는 담지 않습니다** — 점수와
  항목 이름까지입니다.
- ⚠️ `localStorage` 는 사파리 비공개 모드 등에서 **던집니다.** 감싸지
  않으면 화면이 통째로 안 그려집니다. 읽기·쓰기 전부 try/catch.
- 지우는 단추를 반드시 같이 냅니다. `/my` 의 "내 기록 전부 지우기" 는
  되돌릴 수 없으므로 한 번 물어봅니다.
- `/my` 는 **아직 안 하신 칸도 숨기지 않습니다.** 숨기면 무엇을 할 수
  있는 곳인지 모르게 됩니다.

## grid 칸에 글과 태그를 섞지 마세요

⚠️ **이 버그를 세 번 만들었습니다.** `display:grid` 인 칸에 글을 그냥
넣고 그 안에 `<b>` 를 쓰면 **`<b>` 가 딴 칸이 됩니다.** 칸 수를 넘으면
다음 줄로 넘어가 "최대 세 곳" 과 "입니다." 가 다른 줄에 앉고, 칸이 좁으면
글자가 한 자씩 세로로 쪼개집니다. 에러도 안 나고 JS 도 멀쩡합니다.

```
<li>ICON 글 <b>강조</b> 글</li>      ← 칸 2개에 항목 4개. 깨집니다
<li>ICON <span>글 <b>강조</b> 글</span></li>   ← 이렇게
```

- ⚠️ CSS 에서도 같은 일이 납니다. `.diff-r span` 으로 두면 **안쪽
  감싸개까지** grid 가 되어 한 겹 더 깨집니다. `>` 를 쓰세요.
- `check.js` 의 "grid 칸에 글과 태그가 섞임" 이 잡습니다. "아이콘 + 글"
  (칸 2 · 항목 2)은 정상으로 봅니다. 일부러 그런 곳은 class 에 `g-mix`.

### 빈 가짜요소도 칸 하나를 차지합니다

⚠️ 모바일 규칙에 `.diff-r .diff-b::before{ content:"" }` 가 남아
있었습니다. **빈 문자열은 "없음" 이 아닙니다** — 상자가 하나 생기고,
그게 `auto 1fr` 의 첫 칸을 먹습니다. 체크 아이콘이 둘째 칸으로 밀려
혼자 오른쪽 끝에 앉고 글은 통째로 다음 줄로 내려갔습니다.

```
      ✓          ← 아이콘만 딴 줄
고깃집 · 정육점 경험을 확인한 곳에만 …
```

- 지우는 것은 **`content:none`** 입니다. `""` 가 아닙니다.
- ⚠️ `check.js` 는 **못 잡습니다.** 가짜요소는 자식 노드가 아니라서
  "칸 수 대 항목 수" 세기에 안 잡힙니다. 화면을 찍어 봐야 보입니다.

### 뒤에 덧붙인 규칙이 앞의 미디어쿼리를 덮습니다

⚠️ 같은 파일 아래쪽에 새 묶음을 붙일 때 두 번 당했습니다. 우선순위가
같으면 **나중에 쓴 것이 이깁니다 — 미디어쿼리 안이든 밖이든.**

- `.cat-g` 를 뒤에서 `@media (max-width:760px){ 1fr 1fr }` 로 두었더니
  앞쪽 `@media (max-width:520px){ 1fr }` 을 덮어, 390px 에서 카드가
  둘씩 앉은 채 안쪽은 "사진 110px + 글" 가로짜임이라 글 칸이 75px 로
  쪼그라들었습니다. 한 낱말이 여섯 줄이 됐습니다.
- `.cat-ph{ aspect-ratio:4/3 }` 을 뒤에 두었더니 앞쪽 520px 의
  `aspect-ratio:auto` 를 덮었습니다. **높이가 정해진 칸에 비율을 주면
  브라우저가 폭을 거꾸로 계산합니다** — 110px 칸을 넘어 그림이 글자를
  덮었습니다.
- 새 묶음을 붙였으면 **좁은 폭 규칙도 같이 다시 쓰세요.** 앞에 있던
  것이 살아 있을 거라고 믿지 마세요.

## 검사 코드는 템플릿 문자열 안에 있습니다

⚠️ `check.js` 의 `AUDIT` 과 흐름 검사는 **백틱 문자열**로 브라우저에
건네집니다. 그래서:

- **백슬래시-s 를 쓰면 그냥 s 가 됩니다.** 정규식을 쓰려면 백슬래시를
  두 번 쓰거나(`\\s`), 아예 문자열 `split(" ")` 로 피하세요.
  **세 번 당했습니다.** 한 번은 `/s$/` 로 돌아 아무것도 안 잡았고,
  한 번은 칸 수를 늘 1로 세어 124건이 오탐으로 나왔습니다.
- **주석 안에 백틱을 쓰지 마세요.** 문자열이 거기서 끝납니다.
- ⚠️ `img.complete` 나 load 이벤트를 기다리지 마세요. `loading="lazy"`
  인 그림은 화면에 들어오기 전까지 load 도 error 도 안 쏩니다 —
  검사가 **12분 동안 멈춰** 있었습니다. 주소를 `fetch` 로 받아 보세요.

## 구조화 데이터 (JSON-LD)

`build-pages.js` 의 `jsonLd()` 가 화면마다 넣습니다. 구글이 제목·설명
말고 **이 화면이 무엇인지**를 읽는 자리입니다.

| 화면 | 무엇 |
|---|---|
| 메인 | `WebSite`(+검색) · `Organization` · `FAQPage` |
| 글 하나 | `Article` · `BreadcrumbList` |
| 연구소 | `CollectionPage` + `ItemList` |
| 진단 · 창업비 · 견적비교 | `WebApplication` (무료는 화면에도 적힌 사실) |

- ⚠️ **`aggregateRating` 을 넣지 마세요.** 검색 결과에 별이 뜨는데,
  그 별은 실제 후기가 있어야 붙이는 것입니다. 없는 별을 붙이면 지어낸
  숫자를 구글에까지 내보내는 셈이고, 적발되면 리치 결과가 통째로
  막힙니다 — 부산물몰에서 실제로 그랬습니다.
- ⚠️ 구글은 **구조화 데이터가 화면 내용과 같아야 한다**고 못 박아
  두었습니다. 그래서 FAQ 는 `js/data/faq.js` 한 곳에 두고 화면과
  JSON-LD 가 같은 것을 읽습니다. 한쪽만 고치면 어긋납니다.
- ⚠️ 메인(`index.html`)은 build-pages 가 덮어쓰지 않는 파일이라,
  `<!-- ld:start -->` 표시 **사이만** 써 넣습니다. 표시를 지우면
  build-pages 가 멈춥니다.
- `noindex` 화면에는 넣지 않습니다.

## 도구와 글은 서로 이어져 있어야 합니다

도구는 **해 보는 것**이고 글은 **알려 주는 것**입니다. 둘이 따로 놀면
진단을 받고도 뭘 읽을지 모르고, 글을 읽고도 뭘 할지 모릅니다.

| 어디서 | 어디로 | 무엇으로 |
|---|---|---|
| 진단 결과의 약한 항목 | 그 주제 글 | `WOW_CHECK[].post` |
| 창업 단계 | 그 단계 글 | `WOW_STARTUP_STEPS[].post` |
| 글 끝 | 바로 써 볼 도구 | `WOW_POSTS[].tool` |
| 진단 결과 | 도구 모음 | `/check` 결과의 "숫자로 더 보기" |
| 도구에 적은 것 | MY BUSINESS | `ylBrief()` · `bepBrief()` |

- ⚠️ **억지로 붙이지 마세요.** 맞는 글이 없으면 비웁니다. 엉뚱한 글로
  보내면 다음부터 안 누릅니다 (진단 8항목 중 "고정비" 는 비어 있습니다).
- ⚠️ 없는 글이나 없는 주소를 가리키면 손님이 404 를 봅니다.
  `check.js` 가 **가리키는 글이 전부 있는지**, **도구 주소가 전부
  열리는지**를 봅니다.

## 접수가 실패했을 때가 더 중요합니다

⚠️ 여태 토스트 한 줄로 끝났습니다. 전화번호도 아직 없어서 손님은 길게
적은 글을 들고 **막다른 길**에 섭니다. 다시 적으라고 할 수는 없습니다.

`sendFail(칸id, 다시시도, 복사할글)` 이 (1) 적으신 것을 화면에 그대로
두고 (2) 복사해 둘 수 있게 하고 (3) 다시 시도를 주고 (4) 전화가 있으면
전화를 줍니다. SOS · 견적 요청 · 파트너 등록 세 곳이 같이 씁니다.

- ⚠️ **화면을 다시 그리지 마세요.** 다시 그리면 적으신 글이 날아갑니다.
  안내만 폼 위의 빈 칸에 넣습니다.
- ⚠️ `navigator.clipboard` 는 https 가 아니거나 오래된 브라우저에서
  없습니다. 옛 방법(`execCommand`)으로 떨어지게 해 두었습니다.
- ⚠️ **이 화면은 실패해야 나옵니다.** 그래서 평소 전수 점검에는 아예
  안 잡힙니다 — `check.js` 가 일부러 실패시켜서 봅니다. 실제로 이
  상태에서만 CSS 선택자 실수로 문장이 깨진 적이 있습니다.

## `>` 를 빼면 문장 한가운데가 깨집니다

⚠️ **CSS 선택자를 넓게 쓰다가 네 번 겪었습니다.** `.notice-bad b` 처럼
자식 기호 없이 쓰면 제목용 `<b>` 뿐 아니라 **문장 안의 `<b>` 까지**
덩어리(flex/block)가 되어 그 낱말만 딴 줄에 앉습니다.

```
적어 주신 내용은
그대로 남아 있습니다.          ← 이렇게 됩니다
잠시 뒤 다시 눌러 보시거나 …
```

`check.js` 의 "grid 칸에 글과 태그가 섞임" 이 두 가지를 같이 봅니다 —
grid 칸 넘침과 **문장 속 덩어리**. 일부러 그런 곳은 class 에 `g-mix`.

## 확인

```bash
node check.js          # 29개 화면 × 1440·1024·390px 전수 (playwright 필요)
node tools/test-api.js # 접수 주소가 화면이 보낸 칸을 빠짐없이 전달하는가
node tools/make-img.js # img/*.svg 를 다시 그립니다
node build-pages.js    # 주소마다 HTML + sitemap
node tools/shot.js "/|1440|home"   # 화면을 찍어 봅니다 (눈으로 볼 것)
```

보는 것: JS 에러 · 못 불러온 파일 · 가로 스크롤 · **12px 미만 글씨** ·
**40px 미만 누름** · 낱말 가운데 잘림 · 손님 화면에 남은 개발자 말 ·
링크가 실제로 열리는지 · **주소가 화면에 반영되는지** · **SOS 흐름**
(동의 없이 안 보냄 · 분류는 하나만 · **지어낸 실적 숫자가 없는지** —
`n+` 꼴 · 만족도 · 누적 · 별점 · 실적 수식어 다섯 꼴) ·
**줄바꿈이 사라질 때 낱말이 붙는지** · **grid 칸에 글과 태그가 섞였는지** ·
**화면마다 제목(h1)이 딱 하나인지** · **알림이 보이고 들리는지** ·
**새 화면 흐름 16개** · **화면이 서로 이어지는가 18개** ·
**접수 실패 5개** · **도구와 글 6개** · **접수처 없음 5개** ·
**도구 계산 9개** · **머리말 3개** · **고민 가이드 6개**.

⚠️ 화면 하나하나가 되는 것과 **눌렀을 때 다음 화면으로 값이 넘어가는
것**은 다릅니다. 끊기면 손님은 같은 것을 두 번 적게 되고 거기서 닫습니다.
그래서 이어지는 것을 따로 봅니다 — 메인 입력창 → SOS, 고민 카드 →
분류 켜짐, 업체 찾기 → 요청서 서비스, 진단 결과 → MY · SOS 미리채움,
창업 체크 → MY, 창업비 → MY 합계, 견적 비교 → MY, 읽던 글 → MY,
MY 전부 지우기, 연구소 분류 탭, 글 체크리스트, 검색이 여러 종류를 같이
찾는지, 못 찾았을 때 물어보는 길이 있는지, 지역이 요청에 실리는지.

⚠️ `check.js` 의 흉내 서버 MIME 표에 **`.svg` 를 빠뜨리면** 그림이
`application/octet-stream` 으로 나가서 브라우저가 그리지 않고 alt
글자만 보여 줍니다. 화면은 멀쩡해 보이고 JS 에러도 안 납니다 —
실제로 그렇게 봤습니다.

⚠️ "개발자 말 노출" 에 **`지시서`** 를 넣었습니다. 파트너 안내 화면에
"최대 세 곳입니다 (지시서 12번)" 이 그대로 나간 적이 있습니다.

⚠️ **화면을 새로 만들면 `check.js` 의 `PAGES` 에 넣으세요.** 연구소 글
열두 편이 한동안 검사에 **아예 안 들어가 있었습니다** — 제목(h1) 검사를
일부러 깨 봤는데 안 걸려서 알았습니다. 검사가 통과한다는 말이 그 화면을
봤다는 뜻은 아닙니다.

바꾼 화면은 **데스크톱과 모바일(390px) 둘 다** 실제로 띄워 보세요.
새 검사를 넣으면 **일부러 되돌려서 실제로 잡히는지 확인**하세요 — 이
저장소에서 그렇게 안 했다가 통과만 하고 아무것도 안 잡는 검사를 두 번
만들었습니다.

## 가리키는 곳이 실제로 있는가

파일을 지우거나 이름을 바꾸면 `sitemap.xml` · `robots.txt` ·
`manifest.json` 을 같이 보세요.

### canonical 은 화면마다 **하나**여야 합니다

⚠️ **여기서 한동안 틀린 것이 나가고 있었습니다.** `index.html` 에
메인용 canonical 을 손으로 박아 두었는데, `build-pages.js` 가 화면마다
canonical 을 하나 더 **덧붙이고** 있었습니다. 결과는 화면마다 두 개 —
첫째는 전부 `https://aboutmeat.co.kr/` 였습니다.

- 구글은 서로 다른 canonical 이 둘이면 **둘 다 무시**합니다.
- 먼저 오는 것만 읽는 크롤러에게는 **모든 화면이 메인의 복제본**입니다.
- ⚠️ **브라우저로 봐서는 표가 안 납니다.** 화면을 그리고 나면
  `paintMeta()` 가 **첫 번째** canonical 을 제 주소로 고쳐 놓기
  때문입니다. 크롤러가 읽는 것은 고쳐지기 전의 HTML 입니다.

그래서 `shell()` 은 덧붙이지 않고 **갈아 끼우고**, 만든 직후에 개수를
세어 하나가 아니면 멈춥니다. `check.js` 의 **머리말** 묶음은 화면을
보지 않고 **파일을 그대로 받아서** 셉니다 — 렌더된 DOM 을 보는 검사로는
이 종류를 영영 못 잡습니다.

- `og:image` 는 **주소 전체**여야 합니다. `/og.jpg` 로 두면 카카오톡·
  페이스북 미리보기에 사진이 안 뜹니다.
- `rel="icon"` 을 안 적으면 브라우저가 `/favicon.ico` 를 찾습니다.
  파일이 없으면 **모든 화면에서 404 가 한 번씩** 납니다.
- `manifest.json` 의 `shortcuts` 에 해시 주소를 쓰지 마세요.
- ⚠️ **`sw.js` 를 지우지 마세요.** 예전 사이트의 서비스 워커가 손님
  브라우저에 설치돼 있습니다. 404 가 되면 브라우저는 "갱신 실패" 로
  보고 **옛 워커를 그대로 계속 씁니다.** 지금 `sw.js` 는 캐시를 비우고
  스스로 등록을 해제하는 일만 합니다.

## 약관과 개인정보처리방침

중개·매칭 서비스 기준으로 다시 썼습니다. 예전 본문은 쇼핑몰 약관이라
청약철회·배송·신선식품 이야기가 그대로 있었습니다 —
`git show c65c5a7:js/data/legal-terms.js` 로 꺼내 볼 수 있습니다.

⚠️ **제일 중요한 조문은 약관 제5조(회사의 지위)입니다.** 전자상거래법
제20조 제1항이 **통신판매중개자는 자신이 거래당사자가 아니라는 사실을
미리 고지**하도록 정하고 있고, 고지하지 않으면 제20조의2에 따라
연대책임을 집니다. **이 조를 빼지 마세요.**

⚠️ **약관에 지키지 못할 약속을 적지 마세요.** "24시간 내 매칭" 을
약관에 적으면 그때부터 채무입니다. 제8조에 "회신 시점을 보장하지
않습니다" 라고 적어 두었습니다.

⚠️ 방침 제4조(제3자 제공)와 요청서의 동의 상자는 **같은 말을 해야
합니다.** 한쪽만 고치면 화면과 문서가 어긋납니다. `check.js` 가 방침에
"별도의 동의" 와 "Vercel" 이 있는지 봅니다.

- **신원 정보를 본문에 박지 마세요.** 상호·대표·사업자등록번호는 문서 맨
  끝 표 한 곳에서만 `WOW_BIZ` 를 읽어 보여 줍니다.
- ⚠️ **`trustees` 를 비워 두면 화면이 "위탁하고 있는 업무가 없습니다"
  라고 단정합니다.** 이 사이트는 Vercel 에 올라가 있고 요청이 그 서버를
  지나갑니다 — 제26조상 수탁자이고, 미국 회사라 제28조의8상 국외
  이전이기도 합니다. **호스팅을 옮기면 같이 고치세요.**
- **이 두 문서는 제가 쓴 초안입니다.** 영업을 시작하기 전에 변호사나
  한국소비자원 표준약관과 대조해 확인하세요.

## 아직 안 된 것

| 항목 | 메모 |
|---|---|
| **백엔드** | 로그인 · MY · 견적 비교 화면 · 관리자가 여기에 걸려 있습니다. 고리의 Supabase 스키마가 새 Entity 와 상당 부분 겹칩니다 (`git show 93336cb^:db/`) |
| 사장님 연구소 | 글 12편과 `/lab` · `/lab/:slug` 가 실제로 돌아갑니다. 글이 없으면 메인에서 구간째 빠집니다 |
| 견적 비교 (`/quotes`) · MY | 받은 견적을 나란히 놓으려면 저장할 곳이 필요합니다 |
| 관리자 페이지 | 지시서 35번 — 초기에는 사람이 분류하므로 매우 중요합니다 |
| **접수처** | `api/quote.js` 가 받아서 밖으로 보내기만 합니다. Vercel 환경변수를 채우고 `WOW_BIZ.sosReady` 를 켜야 실제로 접수됩니다 |
| **사업자 정보** | **사장님이 맨 마지막에 넣기로 하셨습니다 (2026-09-26).** 그때까지 푸터·약관의 신원 줄과 전화 버튼은 안 나옵니다 — 자리표시자를 채워 넣지 마세요. 영업 시작 전에는 반드시 필요합니다 (전자상거래법 제10조) |
| 약관·방침 검토 | 중개·매칭 기준으로 다시 썼습니다. **초안이므로 영업 전에 확인하세요** |
| **실제 사진** | **사진이 생길 때까지 그림으로 갑니다 (2026-09-26 결정).** 지금 `img/` 의 41장은 코드로 그린 그림입니다. 자리 목록은 `img/README.md`. 사진이 준비되면 `js/data/photos.js` 의 경로만 바꾸면 됩니다 |
