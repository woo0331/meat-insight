# 고리 — 켜는 순서

개발은 다 끝났습니다. 여기 적힌 것만 하면 서비스가 열립니다.
**위에서부터 순서대로** 하세요. 앞 단계를 건너뛰면 다음 단계가 실패합니다.

각 단계 끝에 "이렇게 되면 성공"을 적어 뒀습니다. 그대로 안 되면
다음 단계로 넘어가지 마세요.

---

## 0. 준비 — 지금 상태 확인 (5분)

브라우저로 `db-check.html` 을 엽니다. 지금은 빨간불이 많을 겁니다.
**무엇이 빠졌는지 목록으로 보여 주므로, 1~4단계를 마친 뒤 다시 열어
전부 초록불이 되는지로 확인하면 됩니다.**

> `db-check.html` 은 `robots.txt` 로 검색 차단·`noindex` 처리돼 있어
> 남에게 노출되지 않습니다.

---

## 1. DB 표 만들기 (15분) 🔴 제일 먼저

Supabase → 왼쪽 메뉴 **SQL Editor** → New query →
아래 파일 내용을 **순서대로** 붙여넣고 각각 Run 합니다.

| 순서 | 파일 | 무엇이 생기나 |
|---|---|---|
| 1 | `db/phase2_schema.sql` | 요청 · 견적 · 후기 · 당일알바 |
| 2 | `db/phase3_schema.sql` | 알림 · 채팅 · 인증 · 거래 · 시세 |
| 3 | `db/phase4_admin.sql` | 관리자 계정 · **RLS(2단계에서 켭니다)** |
| 4 | `db/phase5_storage.sql` | 업체 사진 저장소 |
| 5 | `db/phase7_report.sql` | 신고 · 문의 창구 |
| 선택 | `db/phase6_realtime.sql` | 새 견적·메시지가 새로고침 없이 뜸 |

전부 **추가 전용**입니다. `DROP`·`DELETE`·`TRUNCATE` 가 한 줄도 없고,
여러 번 실행해도 안전합니다. 기존 데이터는 건드리지 않습니다.

> ✅ **이렇게 되면 성공** — `db-check.html` 에서 표 19개가 전부 초록불.

---

## 2. RLS 켜기 (5분) 🔴 이걸 안 하면 아무나 데이터를 지웁니다

지금 상태로 두면 **사이트에 공개된 anon 키만으로 남의 요청·업체 정보를
지울 수 있습니다.** 실제로 확인했습니다 — 임시 DB 에서 anon 으로
`delete from purchase_requests` 를 하니 전부 지워졌습니다.

**`db/RLS_ON.sql` 파일을 통째로 복사해서 SQL Editor 에 붙여넣고 Run.**
그게 전부입니다. 주석을 풀 필요도, 순서를 맞출 필요도 없습니다.

- 없는 표는 조용히 건너뜁니다 (1단계를 아직 안 했어도 실행됩니다)
- 여러 번 실행해도 같은 결과입니다
- **읽기와 등록은 계속 열려 있습니다** — 손님이 요청을 올리는 것도,
  목록을 보는 것도 그대로 됩니다. 막는 것은 "남의 것 수정·삭제" 뿐입니다
- 맨 아래 🔴 표시된 곳에 **본인 이메일을 관리자로 등록**하는 줄이 있습니다.
  등록하지 않으면 삭제할 수 있는 사람이 아무도 없게 됩니다

> ⚠️ 실행하면 구버전 관리자 페이지(`suppliers.html` · `jobs.html` ·
> `purchase_request.html`)의 수정·삭제가 멈춥니다. 대신 `admin.html` 을
> 쓰세요 — 관리자 로그인 기반입니다.
>
> ✅ **이렇게 되면 성공** — 실행하면 맨 아래 확인표가 나옵니다.
> **19개 표가 전부 `✅ 켜짐` 이고 "정책" 이 1 이상이면 됩니다.**
> `❌ 꺼짐` 이 하나라도 있으면 그 표가 아직 무방비입니다.
> 그 뒤 사이트에서 요청 등록·견적 보내기가 여전히 되는지 한 번 해 보세요.

## 3. 사업자 정보 채우기 (5분) 🔴 법으로 표시해야 합니다

`site-info.js` 를 열면 맨 위에 있습니다. 지금은 푸터에 **(미기재)** 로
보입니다.

```js
window.GORI_BIZ = {
  company:       "",   // 상호 (법인명 또는 개인사업자 상호)
  ceo:           "",   // 대표자 성명
  brn:           "",   // 사업자등록번호  000-00-00000
  mailOrder:     "",   // 통신판매업 신고번호  제0000-지역-0000호
  address:       "",   // 사업장 주소
  phone:         "",   // 고객센터 전화번호
  email:         "",   // 문의 이메일
  privacyOfficer:"",   // 개인정보 보호책임자 (직책·성명)
  hostRegion:    ""    // 데이터 보관 리전 — Supabase 프로젝트 리전
};
```

여기 한 곳만 채우면 푸터·이용약관·개인정보처리방침에 **모두** 반영됩니다.

상호·대표자·주소·전화·이메일·사업자등록번호·통신판매업 신고번호는
전자상거래법 제10조상 **표시 의무**입니다. 개인정보 보호책임자는
개인정보 보호법 제31조상 지정·공개 의무입니다.

> ⚠️ 제가 임의로 채우지 않았습니다. **확인되지 않은 번호를 적으면
> 그 자체가 표시 의무 위반**이라, 실제 등록된 값만 넣으세요.
>
> ✅ **이렇게 되면 성공** — 푸터 맨 아래에서 (미기재) 가 전부 사라집니다.

---

## 4. 카카오 로그인 (20분) 🟡

**코드는 이미 다 돼 있습니다.** 설정만 하면 켜집니다.
사이트에 카카오 키를 넣지 않습니다 — Supabase 대시보드에만 넣습니다.

1. [카카오 개발자센터](https://developers.kakao.com) → 앱 만들기 →
   **REST API 키**와 **Client Secret** 발급
2. 카카오 앱 → 카카오 로그인 → **Redirect URI** 에 아래를 등록
   ```
   https://igignylgzjphtnyxhhsd.supabase.co/auth/v1/callback
   ```
3. 카카오 앱 → 동의항목에서 **닉네임 · 이메일**을 필수로 설정
4. Supabase → Authentication → **Providers → Kakao** 켜고 위 두 키 입력
5. Supabase → Authentication → **URL Configuration** →
   Site URL 에 `https://aboutmeat.co.kr` 입력

> ✅ **이렇게 되면 성공** — 로그인 창의 "카카오로 시작하기" 를 누르면
> 카카오 동의 화면이 뜹니다.
> 설정이 덜 됐으면 **화면에 무엇이 빠졌는지 그대로 알려 줍니다** —
> "준비 중" 같은 애매한 문구는 나오지 않습니다.

---

## 5. 배포 (5분)

Vercel 에 이 저장소를 연결하면 끝입니다. 빌드 도구가 없는 정적
사이트라 빌드 설정이 필요 없습니다.

- Framework Preset: **Other**
- Build Command: **비워 두기**
- Output Directory: **비워 두기**

도메인은 Vercel → Settings → Domains 에서 `aboutmeat.co.kr` 연결.

> ✅ **이렇게 되면 성공** — 주소로 들어가서 요청을 하나 올려 보고,
> 거래관리 → 내 요청에 뜨는지 확인.

---

## 6. 나중에 해도 되는 것 ⚪

| 무엇 | 어디서 |
|---|---|
| **예시 데이터 끄기** — 진짜 요청이 올라오면 자동으로 사라지지만, 확실히 끄려면 | `site-info.js` → `GORI_FEATURES.demo` |
| 홈 뉴스·인사이트 채우기 (비워 두면 "준비 중"으로 보입니다) | `site-info.js` → `GORI_CONTENT` |
| 오늘 시세 값 넣기 (없으면 시세 칸이 숨겨집니다) | Supabase `market_prices` 표 |
| 관리자 계정 지정 | `db/phase4_admin.sql` 안내 참고 |
| 컨설팅 결제를 실제로 할지 결정 (지금 테스트 키) | `meat_insight_apply.html` |
| 약관·개인정보처리방침 한 번 읽어보기 | `terms.html` · `privacy.html` |
| 고리페이(안심결제) — 전자금융업 등록이 필요합니다 | `site-info.js` → `GORI_FEATURES.pay` |

---

## 자주 막히는 곳

**"서버에 연결할 수 없습니다"**
→ Supabase 프로젝트가 일시정지됐을 수 있습니다. 대시보드에서 Resume.

**요청은 올라가는데 견적이 안 보임**
→ `phase2_schema.sql` 이 안 돌아갔습니다. 1단계로.

**로그인은 되는데 거래관리가 비어 있음**
→ RLS 정책이 너무 빡빡하게 걸렸습니다. `db-check.html` 이 어느 표에서
   막히는지 알려 줍니다.

**화면이 예전 모습으로 보임**
→ 브라우저 캐시입니다. Ctrl+Shift+R (Mac 은 Cmd+Shift+R).

---

## 개발자에게 넘길 때

`CLAUDE.md` 에 **코드를 고칠 때 지켜야 할 규칙**이 적혀 있습니다.
특히 이 세 가지는 어기면 조용히 망가집니다.

1. `gori-app.js` 를 직접 고치지 말 것 — `src/` 를 고치고 `node build.js`
2. `index.html` 을 고치면 반드시 `cp index.html meat_insight_main.html`
3. 고친 뒤 `node test/run.js` (회귀 35종) 와 `test/contrast-e2e.js` 실행
